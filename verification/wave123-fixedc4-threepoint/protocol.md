# Wave 123 verifier protocol

Role: independent verifier.

## Frozen target

Audit the exact projector, two-by-two graph-valued completion, and rooted
three-point claims for the displayed Wave120 coordinate families and the
sealed explicit 26-record subset.

## Independence

- Freeze and preinspect the discovery manifest before reading the package.
- Import no discovery checker.
- Rebuild all 99-coordinate signed vectors from the sealed Wave120 witness.
- Invert every support Gram matrix over `Fraction`.
- Verify the span-projector factorization, rank, and action on its columns.
- Classify every coordinate pair against both allowed graph projector values.
- Reconstruct each rooted PSD block as an explicit feature Gram `F F^T`.
- Preserve the distinction between code geometry and a common graph
  eigenspace.

## Search and status wall

The retained restart search has no exhaustive tree or certificate.  Its
telemetry may describe an attempted heuristic but cannot establish a cap.
Claims about another 26-word family, ranks 28 and 30, Conway-99, and novelty
remain `UNKNOWN`.
