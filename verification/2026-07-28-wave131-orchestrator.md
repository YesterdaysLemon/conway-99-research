# Wave 131 orchestrator decision

Decision: publish the sealed Waves 123 and 127--131 packages as scoped
research boundaries.  Do not promote any of them to a rank exclusion,
construction, strict general upper bound, or Conway-99 resolution.

## What changed

Three broad relaxations now have explicit exact survivors:

1. fixed-C4 pairwise and rooted code geometry admits 26 records;
2. the full index-10/index-70 Jacobi cone is rationally feasible through
   Fourier cutoff 20; and
3. the ordinary binary MacWilliams cone admits the complete forced
   size-three distributions with distances `14/15`.

The finite-primary motif shift is exact but excludes no rank row.  The
projector lane also identifies a stronger discriminator: one common graph
projector must simultaneously choose edge/nonedge entries and remain
positive semidefinite.

## Priority order

1. **Exact cutoff-28 Jacobi LP.**  Seek either a rational point replayed
   against every original row or an exact Farkas certificate.  Floating
   feasibility and failed rational reconstruction have no theorem status.
2. **Graph-valued projector completion.**  Combine the allowed projector
   entries with degree 14, the `lambda=1`, `mu=2` equations, and selected
   eigenvector equations.  Diagonal leverage or code-only three-point PSD
   is insufficient.
3. **Labelled joint binary enumerator.**  Retain the 99 weight-14 rows with
   Gram `A`, the 99 weight-15 dual rows with Gram `I+A`, and their mutual
   orthogonality.  Another ordinary one-variable enumerator is deprioritized.
4. **Finite discriminant continuation only with a global ingredient.**
   Local Smith factors, Gauss phases, and Witt type alone are now a proved
   null boundary for every live row.

## Publication boundary

The public checkpoint should include the complete sealed discovery and
verifier packages, including failed attempts and `UNKNOWN` reports.  The
unsealed exact-LP continuation remains local until it produces a sealed
replayable result.

```text
project status: EXPLORATORY
resolution claim: none
rigorous n3 interval: 708 <= n3 <= 4158
Conway-99: UNKNOWN
```
