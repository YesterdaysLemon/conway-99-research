# Waves 46--49 orchestrator decision

Date: 2026-07-27 UTC

## Decision

Publish the independently checked finite claims from Waves 46 through 49 as
`VERIFIED_SCOPED`. For Wave 48 this promotion covers only the exact affine
rank/nullity and eleven complete universal kernels; its numerical scout stays
`UNKNOWN`, with no feasibility promotion.

In particular:

- the ordinary `F7` code lane is a verified null obstruction;
- the three-root and five-root exact directions refute the 17 stored
  aggregate count witnesses, not the full count region;
- the 13 branch-15 XOR relations are valid in their seven stated local
  degree-two windows and do not close the branch;
- all solver statuses, tiny margins, floating duals, and timeouts remain
  non-evidentiary.

Do not publish any endpoint nonexistence claim, strict upper bound below
4158, graph construction, or novelty/priority claim.

## Strategy decision

The same-order moment hierarchy is now close to exhausted: five labelled
roots plus one free vertex is the strongest Gram layer using only order-six
and order-seven variables. Its near-boundary numerical behavior makes two
continuations prudent:

1. obtain an exact conic certificate by rational facial reduction and
   proof-producing linear/conic dualization, or exhibit an exact feasible
   moment vector;
2. move to a structurally different space retaining local compatibility,
   especially the triangle-incidence hypergraph and its signed
   intersection equations;
3. inject the verified local XOR relations into degree-four or
   branch-conditioned degree-three proof-producing search; and
4. bridge moment cuts to the explicit simultaneous `B/H` completion instead
   of adding further aggregate witnesses.

## Status wall

```text
new unconditional theorem:             NONE
complete endpoint case closed:          NONE
endpoint proof coverage:                0/33
general upper bound:                    n3 <= 4158
rigorous interval:                      708 <= n3 <= 4158
Conway-99:                              UNKNOWN
```
