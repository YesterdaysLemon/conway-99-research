# Clean-room protocol

1. Reconstruct the exact-three flag relation and fixed-center `A_x`
   injection from frozen Wave180 before reading Wave195.
2. Derive pairwise intersection of fixed-center `A_x` three-subsets directly
   from the weight-three relation that disjoint members would create.
3. State every Hilton--Milner hypothesis and evaluate its exact
   `n=7,k=3` formula.
4. Treat the common-star case separately, proving the `p`-leaf and `q`-leaf
   are distinct and auditing the union bound `12+12+c_x<=39`.
5. Form one simple exact-three flag family from the selected, old, and new
   companion-closed pools using the already verified separation theorems.
6. Count distinct oriented labels. In particular, audit the two type-two
   endpoint orientations for one private undirected nonedge and separation
   across different private sources and from the selected-type-three union.
7. Derive `J<=3861`, the lower bound for `J`, and the resulting `SG` row.
8. Reconstruct the exact rational coefficient identity and independently
   check integer rounding and the edge/scalar consequences.
9. Reconstruct a rational equality control and label it arithmetic only.
10. Quarantine the failed mixed type-one orientation route: an `a3`
    assignment contributes one orientation, never an unproved factor two.
11. Freeze the independent mathematical result before opening either
    Wave195 source package.
12. After the freeze, validate source hashes, compare both accounts, replay
    their checkers, and retain the conditional boundary.
