# Independent verification protocol

1. Validate the Wave 186 source package, its input freeze, and every file
   named by the four direct verified input manifests.
2. Do not import or execute the discovery checker.
3. Separate the multiplicity classifications from the later `Q=2079`
   equality consequences.  Accept only classifications already verified
   under the endpoint premises.
4. Reconstruct all star translations coefficientwise over `F_3`.
5. Prove that a translated relation contains a cross circuit using
   proper-star independence, not merely the existence of a dependency.
6. Declare a forced circuit outside the selected cover only for a private
   label and only after proving it differs from the selected circuit.
7. Permit maximum overlap: different private labels may share companions or
   translates, and one outside circuit may receive three assignments.
8. Treat the distinct triple companions as another inequality on the same
   outside pool, not as a disjoint population.
9. Recompute the coefficient combination and all scalar factors exactly.
10. Keep endpoint existence, rank-11 exclusion, novelty, and Conway 99
    `UNKNOWN`.
