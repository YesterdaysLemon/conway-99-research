# Wave 14 detached clean-clone replay

```yaml
role: orchestrator_release_gate
date_utc: 2026-07-23T10:10:54Z
tested_commit: 886f0a73cd4b0cb3ceffb1e8077021f73e7278ea
discovery_commit: e3edcfbe188f9504d9f259993bc89a2296153529
verification_commit: 6b6d721885a6d7c8451c6d6322a35e881b72d94e
verdict: PASS
target_result: UNKNOWN
novelty: UNKNOWN
environment:
  platform: Microsoft Windows NT 10.0.26200.0
  python: 3.13.14
  git: 2.51.0.windows.1
test_counts:
  tracked_code: 52
  focused_wave13: 12
  tracked_verification: 104
  wave14_proof: 10
  wave14_computation: 7
```

## Isolation protocol

A fresh local clone was created from the repository object database with
`--no-local` and checked out detached at exactly
`886f0a73cd4b0cb3ceffb1e8077021f73e7278ea`. The source worktree's untracked
Wave 15 files were therefore absent. The clone was clean before replay and
remained clean afterward. `git fsck --no-dangling --no-progress` passed.

The temporary clone was created at a fixed, prechecked path under the Windows
temporary directory. After its exact path, detached head, and clean status
were rechecked, it was moved to the Recycle Bin. The operation is recoverable.

## Replay results

The detached clone passed:

- YAML parsing and the Wave 14 `UNKNOWN` equality-status assertion;
- 52 tracked `code/test_*.py` tests;
- 12 focused Wave 13 current-bundle tests;
- 104 tracked `verification/test_*.py` tests;
- 10 focused Wave 14 proof/countermodel tests;
- 7 focused Wave 14 computation/provenance tests;
- the independent Wave 14 proof reconstruction;
- the independent countermodel reconstruction; and
- the independent computation audit.

Four artifacts were regenerated byte-for-byte:

| artifact | SHA-256 |
|---|---|
| `attempts/wave14-proof-a/all2-active-countermodel.json` | `5d06ae777fb0ca4450951708bba3ffc85d1f617be1d19c2be210f43df82b8952` |
| `attempts/wave14-computation/n3-48-profile-census.json` | `c2acf43d29aa08179869a31327991336ac54559fa5ec4915d9f3ea993de7bb5a` |
| `attempts/wave14-computation/n3-48-independent-validation.json` | `20ac2afa64329074677f698331460a27e987ff74527e99ceeee5e5f01fc6d069` |
| `verification/n3-48-computation/independent-audit-results.json` | `b19959c358526b710ed1b7e7beff477ddbc35b975609ab1ccd917ccb0f0a5ae4` |

## Preserved pre-release failures and repairs

The first release gate found that the computation-audit report's declared
JSON hash no longer matched the file after replay. The independent audit had
embedded a live `date_utc`, so every successful run rewrote its own evidence.
Before publication, the volatile field was replaced by the fixed
`audit_frozen_utc` identifier. Two separate processes then produced the same
bytes, and the full gate matched all 65 declared path hashes. The final
verifier-source hash is
`e0199aaa481b632104ab200496bf2b20efed529a93901a0d401220e062f66d51`;
the final result hash is the `b19959...a5ae4` value above.

A local pre-clone suite invocation also passed tracked file paths directly to
`unittest`, causing Python to resolve `code` as its standard-library module.
Five loader errors occurred and zero tests ran. The established discovery
commands were then used, producing the passing 52- and 104-test results above.
This was a harness invocation failure, not a mathematical or test assertion
failure.

The first all-in-one clone wrapper was rejected before execution because the
shell safety policy would not accept a computed recursive cleanup target.
Replay was rerun at a fixed explicit path. A later direct recursive cleanup
was also rejected; the verified temporary clone was instead moved to the
Recycle Bin. No safety guard was bypassed.

## Status boundary

This replay verifies the repository mechanics and the scoped Wave 14
artifacts. It does not exclude `n3=48`. The seven negative solver rows remain
`UNSAT_UNVERIFIED`, and the strongest bound at the tested checkpoint remains
`n3>=48` and `induced_C6_count>=209334`. Conway-99 and novelty remain
`UNKNOWN`.
