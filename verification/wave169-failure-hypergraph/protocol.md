# Independent verification protocol

Two hostile paper audits separately checked arithmetic and local
extendability scope.

Checks:

1. reconstruct one-mark completion multiplicity and order;
2. reproduce every global threshold and divisibility equivalence;
3. preserve mandatory versus conditional failure status;
4. restrict mismatch energy to integral one-hot assignments;
5. inspect the root `7K2` degree saturation;
6. inspect the supporting 22-edge cross-shell;
7. list every unverified global SRG closure; and
8. infer only failure of the current pairwise relaxation.

No computation ran below the host memory floor. The verifier cannot promote
the proposed hypergraph or conic continuation without an exact certificate.
