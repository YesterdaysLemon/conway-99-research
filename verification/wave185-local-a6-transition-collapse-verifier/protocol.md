# Independent verification protocol

1. Validate the frozen Wave 185 source manifest and input freeze, then
   validate every file named by each direct prior-package manifest.  Do not
   reinterpret older transitive freezes whose historical formats and mutable
   repository-level inputs differ.
2. Do not import or execute the Wave 185 discovery checker.
3. Reconstruct the alleged prism as an induced six-vertex graph from the
   rooted labels and check that a same-endpoint cell edge is impossible.
4. Derive the two transition rows from the exact residual degree and rooted
   endpoint-profile equations; do not assume an equitable partition.
5. Prove the incident-pair cap separately for each actual-endpoint
   orientation, retaining the sharp proved cap six rather than strengthening
   it without evidence.
6. Keep local companion occurrences distinct only after applying exact
   nonedge-root uniqueness across the five-coclique support.
7. Recompute all congruences, profiles, root counts, and complement triangle
   counts with exact integers.
8. Record any correction explicitly and keep equality, endpoint existence,
   and Conway 99 `UNKNOWN`.
