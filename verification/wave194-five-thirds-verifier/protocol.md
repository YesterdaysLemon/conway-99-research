# Clean-room protocol

1. Reconstruct the theorem from the task statement and the already verified
   Wave181, Wave189, Wave191, and Wave193 premises without reading either
   Wave194 source package.
2. Freeze the independent mathematical result before opening the Wave194
   primary or hostile-audit manifest.
3. Audit the type-two exact-three raw subtraction at the leaf coordinate and
   exclude exact-two residual multiplicity.
4. Prove distinctness when both type-two translates for one private label
   generate residuals.
5. Audit every residual collision with selected, selected-companion, old
   exact-one, old exact-two, old exact-three, and opposite-companion pools.
6. Split the new residual closure as `y` exact-one circuits and `g` complete
   exact-three companion pairs, retaining capacities `y` and `3g`.
7. Reconstruct the overlap variable `k` in the type-three low-target row and
   cancel it rather than double-counting selected low circuits.
8. Verify the exact coefficient identity for `Q0-5C/3` and reconstruct every
   equality condition.
9. Compare the frozen independent result with both source packages only after
   the freeze.
10. Treat every null row as arithmetic only and retain `UNKNOWN` status for
    rank 11, endpoint existence, and Conway-99.
