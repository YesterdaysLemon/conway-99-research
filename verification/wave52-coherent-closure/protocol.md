# Wave 52 independent verification protocol

1. Freeze the complete discovery package and discovery-agent report before
   reading or comparing generated matrices.
2. Do not import the discovery implementation.
3. Re-derive the one-root arithmetic directly from
   `srg(99,14,1,2)` and the prism-free endpoint:
   `3K6`, global relation valencies `(1,18,32,144,36)`, and cross-sector
   petal degrees `B2/C4/D0`.
4. Rebuild the 19-node rooted relation object from typed nodes.
5. Rebuild the 163-node completion-free object from 19 triangle nodes,
   108 candidate-`B` nodes, 36 exact-two cap nodes, and their semantic
   incidences.
6. Run an independent directed ordered-pair 2-WL implementation. Preserve
   multiset multiplicities and verify that every stable color has constant
   intersection numbers.
7. Construct each of the four simple bipartite 2-factor cycle types and all
   64 canonical triples. Check every one of the 36 cap equations.
8. Compare all 64 color counts, refinement depths, and all 4,096 pairwise
   fingerprint-equivalence decisions against the frozen discovery result.
9. Require hostile mutations to be rejected: a deleted `B` edge, a duplicated
   edge or endpoint, a changed cap incidence, and a weakened memory floor.
10. Keep the claim scoped. `Conway-99`, the prism-free endpoint, and novelty
    remain `UNKNOWN`; no graph or improved `n3` upper bound is certified.
