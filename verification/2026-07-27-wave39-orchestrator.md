# Wave 39 orchestrator decision

Date: 2026-07-27 UTC

## Decision

Publish Wave 39 as a rigorous rank and proof-producing checkpoint, not as a
solution or counterexample.

Discovery did not certify itself. Independent verifier packages reconstruct
the edge-local geometry and rank transport, exhaust the 10,395 normalized
matching configurations, replay the branch-15 generalized-unit argument, bind
the exported OPB and proof bytes, and run fresh strict VeriPB checks.

## Mathematical assessment

The strongest new universal theorem is

```text
rank_F7(M) >= 19.
```

It replaces the Wave 38 floor of thirteen and reduces the endpoint arithmetic
rank-pair census from 528 to 429. Its lowest-rank equality type is locally
rigid: if `r7<=20`, every edge must have cycle partition `2+2+2`. This is a
promising coupling target, but no theorem yet proves those edge types cannot
coexist globally.

The proof-producing lane closes one explicit polarity shard:

```text
branch15 AND x187=1 is UNSAT,
so branch15 entails x187=0.
```

This is a genuine checked proof artifact, but it does not close branch 15 or
any complete refined endpoint case. Therefore proof coverage remains `0/33`.

The cross-base and simultaneous `B/H` packages are retained as
`DERIVED_INCONCLUSIVE`. They expose new necessary dependencies and exact
conditional overlap counts, while their positive controls and nonnegative
scheme transforms block status promotion.

## Continuation

The highest-value next routes are:

1. couple the universal edge-local types globally, beginning with the
   equality regime in which every edge has type `2+2+2`;
2. solve or certify the remaining `branch15 AND x187=0` shard, then repeat the
   proof-producing polarity method only where it yields an exhaustive cover;
3. combine the cross-base support-four/six dependencies across the 99 vertex
   stars with the simultaneous `B/H` equations.

## Publication classification

```text
edge-local rank theorem:              VERIFIED
rank_F7(M)>=19:                       VERIFIED
branch15 AND x187=1:                  VERIFIED UNSAT
branch 15 and endpoint coverage:      UNKNOWN; 0/33
cross-base and simultaneous B/H work: DERIVED_INCONCLUSIVE
upper bound below n3<=4158:           NOT PROVED
target and novelty status:            UNKNOWN
```
