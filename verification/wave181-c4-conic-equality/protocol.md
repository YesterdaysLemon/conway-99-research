# Verification protocol

1. Validate every Wave 181 input and package entry.
2. Read the disjoint-block pairing table from frozen Wave 171.
3. Retain the baseline edge `xt` in every outer-star `j` count.
4. Separate the two extra-neighbor blocks using `P=0`.
5. Classify shared-center multiplicity two before invoking Wave 180.
6. Derive the canonical nonedge involution directly from `mu=2`.
7. Promote the checkerboard Gram kernel only when a true circuit is assumed.
8. Derive equality consequences from both minimal-cover inequalities.
9. Double-count every entry of the integer signed Gram.
10. Treat the 36-fold projector sum as a null boundary.
11. Perform no graph or code search and preserve `UNKNOWN` status.

