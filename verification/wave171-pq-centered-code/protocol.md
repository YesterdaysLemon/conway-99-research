# Verification protocol

1. Freeze the full unrestricted `srg(99,14,1,2)` target.
2. Reconstruct the mod-three polynomial identities from the SRG and incidence
   identities, not from discovery-side code.
3. Check every entry class of a row of `X`.
4. Prove the point-star kernel relation and clique recovery without an
   automorphism assumption.
5. Use the distinct notation `M0=21E0`.
6. Determine exactly where the endpoint assumption enters.
7. Compare the resulting row code against Waves 38 and 39 before treating it
   as new.
8. Promote only the algebraic reformulation; retain Conway-99 as `UNKNOWN`.
