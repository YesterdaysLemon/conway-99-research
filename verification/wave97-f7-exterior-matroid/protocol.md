# Wave 97 clean-room verification protocol

1. Freeze every discovery file by path, byte length, and SHA-256 before
   reading the derivation or executing discovery code.
2. Import only the sealed Wave 51 and Wave 80 conclusions; import no Wave 97
   implementation.
3. Reconstruct `R*R`, `R*R*R`, `C*C`, and the degree-zero-through-three
   Hilbert function directly from the Hadamard products.
4. Rebuild the second compound functor, modular rank, self-orthogonality,
   projectivity, row weight, spectrum, and square identity.
5. Derive the compound Smith form from the integer Smith equivalence of `S`.
   Audit `24010^98` through sorted local valuations and independent
   determinantal-divisor controls; do not confuse raw pair products with
   final invariant factors.
6. Reconstruct generalized Hamming-weight bounds from column-matroid
   nullity.
7. Recompute finite orthogonal group orders, projective point orbits, and
   the embedding orbit; use low-dimensional brute force and wrong-sign
   controls.
8. Separate verified Wave 51 prior consequences from genuinely later
   repository consequences. Preserve literature novelty as `UNKNOWN`.
9. Keep rank 28, a strict `n3` upper bound, and Conway-99 unresolved unless
   a separate certificate proves otherwise.
