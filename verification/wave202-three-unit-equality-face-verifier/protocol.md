# Wave202 source-blind equality-face verifier protocol

Frozen before opening the Wave202 proof-A package.

1. Assume only the sealed Wave201 verifier theorem and the conditional
   prism-free rank-11 endpoint.
2. Freeze the face `Q0=7059`.  Multiply the Wave201 certificate by ten
   and derive the exact nonnegative integer budget

   ```text
   3=8SI+12S2+2SE2+7RA+3SL
     +delta+2eta+SM+SF+a1+6b3+2c2+4W.
   ```

3. Characterize every possible three-unit partition analytically.  Do
   not enumerate graph, flag, or local-family configurations.
4. Combine the partition with all raw split identities and the
   definitions of `SE2`, `SL`, `SF`, `delta`, `eta`, and `SM`.  Derive
   explicit formulas for at least `y,a2,r2,n2,q`, together with their
   integrality, parity, and nonnegativity conditions.
5. Expand the local weighted slack value-by-value, treating deficient
   centers and 13-flag Hilton--Milner equality centers separately.
6. Determine whether total slack at most three forces a genuine
   combinatorial contradiction.  Arithmetic or multiplicity-profile
   rows are controls only and must not be called objects.
7. Freeze the independent derivation and its hashes before opening
   Wave202 proof A.
8. After the freeze, replay the sealed primary package.  A later proof-B
   two-center report may be compared only as corroboration, not used as
   an input premise.
9. Promote `Q>=7060` only if an actual contradiction excludes
   `Q0=7059`.  Otherwise preserve the verified bound `Q>=7059` and state
   the precise equality-face stopping point.
10. Preserve rank 11, endpoint existence, strict original `n3`
    improvement, external novelty, and Conway-99 as `UNKNOWN`.

No graph, code, cover, SAT, LP, configuration, family enumeration,
isomorphism, or brute-force search is part of this audit.
