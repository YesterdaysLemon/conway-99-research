# Wave 34 rooted-structural static comparison audit

Verdict: **the released cycle-space and integral rank-16 projector
reparameterization is independently VERIFIED in its exact unrestricted
graph-only scope; no material discrepancy was found**

```yaml
role: verifier
date_utc: 2026-07-24T21:00:06Z
git_commit: NOT_QUERIED_NO_GIT_AUTHORIZED
public_candidate_commit_supplied_by_orchestrator: 52c94e915927f791d30f680d12181a08567248fb
claim_label: VERIFIED
audit_verdict: STATIC_CLEAN_ROOM_COMPARISON_PASS
scope: >-
  Static comparison of the frozen Wave 34 rooted structural candidate
  against the byte-frozen Stage-1 reconstruction. Independently verify the
  exact Wave33 coordinate binding, SNF, full single-column 2-factor census,
  fixed action, 13+14+43 decomposition, both directions of the rank-16
  projector and integral-numerator equivalence, diagonal and modular claims,
  and every scope wall. Candidate code is not imported or executed.
```

## 1. Byte wall and static-only inspection

All release-table hashes, all nine candidate input hashes, the five-entry
candidate inner manifest, and all eight Stage-1 manifest entries validate.
The accepted Stage-1 manifest remains

```text
233a32ac6cb67bcb23b7b269cc6f3f889d943bd5d0714c4f54ead9cbf28c7681.
```

Candidate Python was treated as inert UTF-8 text. The verifier parsed all
three files into Python syntax trees but did not import or execute them.
The source has seven statically visible tests. `check_results.py` contains
one `exec_module` call, which is precisely why its replay was not used as
verification evidence.

No Git command was run. The public candidate commit above is the commit
identifier supplied by the orchestrator; this audit binds the exact released
bytes by SHA-256.

## 2. Exact binding to the frozen Wave 33 labels

The candidate uses cyclic Fano lines; Wave 33 uses sorted XOR lines. The
verifier enumerated all point permutations and found exactly 168 coordinate
isomorphisms. The chosen binding is

```text
candidate point map = (0,1,3,2,5,6,4)
candidate line map  = (0,3,5,6,2,4,1).
```

It induces explicit 14-support and 70-O permutations. After applying them,
the support adjacency and support-to-O incidence agree entrywise with the
exact Wave 33 arrays. Candidate `incident` O labels map to Wave 33
`support_cross_nonedge_completion` copies; candidate `nonincident` labels
map to `support_edge_completion` labels.

The full mapping is machine-readable in `comparison-results.json`; its
SHA-256 is

```text
7975c51cc684af88f807c9a284a95962b322b2e29928346611934a97d6bf0262.
```

This discharges the candidate's strongest coordinate-binding objection.

## 3. Kernel and Smith normal form

In both coordinate systems, the fixed `14 x 70` incidence `P` has rational
rank 13 and one-dimensional left kernel generated by
`(1^7,-1^7)`. The released spanning-tree minor has determinant `+1`
before and after the Wave 33 binding.

A separate verifier-owned Wave 33 spanning tree uses columns

```text
0,2,4,6,7,8,9,10,20,30,40,50,60
```

after deleting row 13; its determinant is also `+1`. Therefore the product
of the thirteen nonzero Smith factors is one, so

```text
SNF(P)=diag(1^13,0),
dim ker(P)=57.
```

The candidate correctly records that this gives no torsion or divisibility
exclusion.

## 4. Independent complete single-column census

The verifier used two methods not imported from the candidate:

1. a line-side transfer recursion; and
2. an exhaustive ordered-pair-of-perfect-matchings census.

For the second method, two perfect matchings determine a relative
permutation on the seven point vertices. Its cycle type is the half-cycle
type of their union. The verifier weighs the two labeled copies of every
doubled incidence edge and divides the ordered edge-colorings by the exact
component factor.

Both methods give

```text
raw weight-14 binary columns:       193,253,756,909,160
underlying capacity-bounded factors:          4,946,952
labeled Pb=2*1 columns:                     574,118,037
line-transfer cached states:                      1,090.
```

The complete 15-class labeled census matches the released JSON exactly:

```text
7                         262,332,336
6+1                        70,294,224
5+2                        91,117,740
5+1+1                       9,667,812
4+3                        76,574,904
4+2+1                      26,163,837
4+1+1+1                       920,304
3+3+1                      11,738,034
3+2+2                      18,854,388
3+2+1+1                     4,004,196
3+1+1+1+1                      69,972
2+2+2+1                     2,148,384
2+2+1+1+1                     227,136
2+1+1+1+1+1                     4,746
1+1+1+1+1+1+1                      24.
```

This verifies every labeled **single column satisfying `Pb=2*1`**. It
does not enumerate compatible 15-column tuples.

## 5. Fixed action and reconciliation with Stage 1

Let

```text
T=P^T P,
P_F=T(T^2-40T+498I)/1960.
```

Direct exact evaluation in the Wave 33 order gives

```text
candidate R   = P_F,
rank(P_F)     =13,
candidate H_R =T-11P_F,
tr(H_R)       =-3,
P H_R         =2J-P-A_S P.
```

For an admissible `B`, Stage 1 used

```text
P_B=BB^T/12-J/28,
P_0=J/70.
```

Hence

```text
candidate E_W =BB^T/12-J/20 =P_B-P_0,
candidate Q_U =I-P_F-E_W    =Stage1 E.
```

The dimensions reconcile exactly:

```text
candidate: 13+14+43=70;
Stage 1:   (13+15-1)+43=70.
```

The fixed 13-space has trace `-3`; the centered B-space contributes
`-14`, giving Stage 1's fixed combined trace `-17`.

## 6. Rank-16 projector equivalence in both directions

On the 43-dimensional joint kernel, the O-O block gives

```text
H^2+H-12I=0.
```

Hollowness forces residual multiplicities `3^27,(-4)^16`. If
`E_-4` is the residual minus-four projector, then

```text
H=H_R-E_W+3Q_U-7E_-4.                              (1)
```

This proves the candidate's forward rank-16 projector statement and
identifies its `E` with Stage 1's `E_-4`.

Conversely, let `L` be symmetric integral with

```text
PL=0, B^T L=0, L^2=147L,
H=(C_star-7BB^T-L)/21
```

hollow binary. Then `E=L/147` is an orthogonal projector supported on
`Q_U`. Hollow recovery forces the diagonal and trace in Section 8, hence
`rank(E)=16`. Exact coefficient reduction returns (1), and the verifier
checked all six blocks on the constant, Fano-image, centered-B,
residual-3, and residual-minus-four summands.

Therefore both implications are valid; the reduction loses no labeled
binary solution.

The continuous dimensions are also correct:

```text
before B, symmetric ker(P) action: 57*58/2 =1653;
after B, symmetric Q_U action:     43*44/2 = 946;
rank-16 projector locus:           16*27   = 432.
```

## 7. Integral numerator and fixed matrix

In Stage-1 notation, the candidate's fixed integer matrix is

```text
C_star
 =21(H_R+3I-3P_F)+21J/5
 =21T+63I-294P_F+21J/5.                            (2)
```

Every entry of (2) is integral. The exact Wave 33-order calculation gives

```text
diagonal: 51^28,57^42;
off diagonal:
  -6^21,-3^84,0^336,3^168,6^714,9^840,12^252.
```

After the explicit coordinate binding, the candidate-order matrix hash is

```text
9bf08bdd279a7c1fc4267e70d4c448afa5063d78e245c0cf970f53ab3c0e9e69,
```

exactly the released hash. The Wave 33-order hash is

```text
cd6f2f1eef9e4919022d56ef1ae3193661aef6506429a862a26e4daa087398b1.
```

Substituting (1) into (2) proves exactly

```text
L=C_star-7BB^T-21H=147E_-4.                        (3)
```

Equation (3) is the direct algebraic bridge between the candidate and
Stage 1.

## 8. Diagonal, positivity, and modular claims

The fixed projector leverage values are

```text
(P_F)_ii=97/490 on 28 support-edge completions;
(P_F)_ii=87/490 on 42 support-nonedge copies.
```

Since every B row has weight three and recovered `H` is hollow, (3) gives

```text
diag L=30^28,36^42,
tr L=2352=147*16.
```

Symmetry and `L^2=147L` make `L` positive semidefinite with spectrum
`147^16,0^54`, so the candidate's two-by-two principal-minor bound is
valid.

The exact modular shadows are

```text
L=C_star       (mod 7),
rank_F7 C_star =5,
C_star^2       =0 (mod 7),
L=-BB^T        (mod 3).
```

They are constraints, not exclusions. The candidate correctly retains both
modular routes as failed contradiction attempts.

## 9. Stage-1 pair census sharpens the column scope

Stage 1 proved that every one of the 21 duplicate-support-label pairs has

```text
(g,r,h,c)=(2,0,0,0).
```

Thus those paired B rows are disjoint in any complete six-block solution.
No full-solution B column can contain both copies, so half-cycle type `1`
is forbidden after the O-O block is imposed.

Consequently the only types that a full-solution column can have are

```text
half cycles: 7,5+2,4+3,3+2+2;
full cycles: 14,10+4,8+6,6+4+4.
```

The number of `Pb=2*1` columns of these four types, surviving this necessary
duplicate-pair rule, is

```text
448,879,368.
```

The other `125,238,669` columns pass `Pb=2*1` but violate this necessary
duplicate-pair rule. This is a useful strict refinement, not a candidate
error: the candidate explicitly scoped `574,118,037` to `Pb=2*1` alone
and denied 15-tuple coverage. The `448,879,368` count remains an upper
domain before the other columns, pair intersections, `L`, and `H` are
imposed; it is not an extendability count.

## 10. Final verdict and status wall

```text
released structural reparameterization: VERIFIED scoped
exact Wave33 coordinate binding:         VERIFIED
SNF and full Pb-column census:            VERIFIED
13+14+43 / 27+43 equivalence:             VERIFIED
rank-16 projector equivalence:            VERIFIED both directions
integral C/L, diagonal, modular claims:   VERIFIED
material discrepancy:                    NONE

binary solution:                          UNKNOWN
complete exclusion:                       UNKNOWN
rooted graph extension:                   UNKNOWN
full rooted endpoint:                     UNKNOWN
n3=708:                                   UNKNOWN
Conway-99 existence/nonexistence:          UNKNOWN
novelty:                                  UNKNOWN
```

The strongest surviving objection is not to correctness but to closure:
the reduction still leaves compatible labeled 15-tuples and quantized
rank-16 projectors unenumerated. No result here is existence or
nonexistence evidence.
