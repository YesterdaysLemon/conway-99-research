# Wave 30 detached clean-clone replay

Date: 2026-07-24

Verdict: **PASS**

Integration commit:
`0a31b62b337153b6d45dbdf1288810cd726c17a3`

Public baseline:
`d6fb0ad0b249d089d2709c43f770ecb74ee2faee`

The replay used a new local clone with both local-clone optimization and
hardlinks disabled, then checked out the integration commit in detached-HEAD
state. The source and clone copies of `README.md` had identical SHA-256
content, different NTFS file identifiers, and hardlink lists containing only
their respective paths. The source and clone commit-object contents also had
the same SHA-256. All regenerated mathematical results were written outside
the clone checkout.

Runtime:

```text
Python 3.13.14
Windows PowerShell 5.1.26100.8875
Git 2.51.0.windows.1
```

## Mathematical suites and historical separation

The current integration tree passed:

| package | tests |
|---|---:|
| repaired general `h=729` discovery | 20 |
| bare `T20` construction discovery | 16 |
| independent bare-construction verifier | 24 |
| **current-tree subtotal** | **60** |

The historical general verifier was not run against repaired discovery bytes.
Verifier-veto commit
`0bc6dc9b90dff6589cfaf9db1a84e2d8242b6321` was extracted separately. Its
unchanged independent and hostile suites passed 32 tests.

The total passing mathematical count was therefore:

```text
current integration tree:       60
historical verifier snapshot:   32
total:                          92
```

Original discovery commit
`091d0a458ab1f96e3b3f491b677c84824bbf8f44` was also extracted separately.
Its recorded failure reproduced exactly:

```text
submitted test exit:       1
tests run:                 0
submitted generator exit:  1
generator output written:  false
stale and committed hashes both present in the diagnostic: true
```

This expected failure is not included in the 92 passing tests and remains
`V30-GEN-001`.

## Deterministic regeneration

Four accepted result files regenerated byte for byte:

| result | SHA-256 |
|---|---|
| repaired general discovery | `cb0a58195506a51ca6a39aaab197a3592344f7aef8b5b0c2af51770c7069ad6c` |
| historical general independent verifier | `2b948591611e9c984985f7bacc5a77c714332fbe42ed5305b84039c621358416` |
| bare-construction discovery | `0d3723ba4c185dc7865858d16bfc6ada99fd87b4e21da616ef1b1d1ad6b67e11` |
| bare-construction independent verifier | `d7e6f18f82045c3e589dfc003c474fa506b59e57b0a02ef277451f2f28ff03b5` |

The original failed generator wrote no output, as required by the preserved
failure record.

## Manifest and central-ledger gates

All 47 entries in the six Wave 30 manifests validated:

```text
attempts/wave30-general-h729/artifact-manifest.sha256
verification/wave30-general-h729/artifact-manifest.sha256
verification/wave30-general-h729/reverification-artifact-manifest.sha256
attempts/wave30-h729-construction/artifact-manifest.sha256
verification/wave30-h729-construction/artifact-manifest.sha256
verification/wave30-literature-audit/artifact-manifest.sha256
```

The central and repository checks also passed:

```text
claims:                               74 unique
obligations:                          68 unique
claim/obligation evidence paths:       0 missing
status path/SHA-256 pairs:            309 matching
central BibTeX keys:                   82 unique
retained Wave 30 source records:       21 covered
tracked Markdown files:               292 checked
local Markdown links:                 285 resolving
Wave 30 release-diff files:             54 UTF-8, LF-only, NUL-free
```

All Wave 30 JSON and YAML parsed. Every changed text file had exactly one
terminal LF and no CR, NUL, or trailing whitespace.

The status wall remained exact:

```text
project_status: EXPLORATORY
resolution_claim: NONE
target_result: UNKNOWN
conditional_n3_lower_bound: 708
n3_708_excluded: false
h_729_row_excluded: false
general classification: VERIFIED_SCOPED_CONDITIONAL
surviving 20+24 type: UNKNOWN
rooted and integrally indecomposable h=729 forms: UNKNOWN
Conway-99 and novelty: UNKNOWN
```

## Exact-blob privacy gate

The privacy check read exact Git blob bytes, not only worktree text. It
scanned:

```text
integration release tree:   887 files, 14,044,263 bytes
unpublished range:            8 commits
range object census:         95 objects
  commits:                    8
  trees:                     27
  blobs:                     60
new blob census:             60 blobs, 1,263,900 bytes
unique release/range blobs: 851
```

The unpublished range was
`d6fb0ad0b249d089d2709c43f770ecb74ee2faee..0a31b62b337153b6d45dbdf1288810cd726c17a3`.
The scan checked the full release tree, every new blob in the range, and all
eight commit messages for OpenAI and GitHub token shapes, AWS access-key
shapes, private-key headers, credentialed URLs, and local Windows or Unix user
paths. It found zero matches.

No tracked PDF, HTML, archive, or office-document payload was present. No blob
exceeded 5 MiB. The largest release blob was the pre-existing structured
Wave 28 simultaneous-neighbor result at 654,824 bytes.

## Repository integrity

The clone had a clean detached status. `git diff --check` and the full
unpublished-range `git diff --check` returned success.
`git fsck --full --strict` returned exit code zero with no output. The checked
integration tree object was
`89866561237c2fad24351ee932f52e4a6de4ad4e`.

## Operational failures retained

The earlier orchestrator preflight failures are retained in the
[Wave 30 correction ledger](2026-07-24-wave30-orchestrator-corrections.md).

One clean-clone integrity wrapper left the revision expression
`0a31b62...^{tree}` unquoted. Windows PowerShell mangled only that final
tree-peel command after clean status, both diff checks, and strict `fsck` had
already returned zero. The wrapper's overall nonzero exit was discarded. A
quoted rerun repeated all integrity checks successfully and returned the tree
object above.

## Scope

This replay verifies reproducibility and publication hygiene for two scoped
Wave 30 results:

1. the conditional fourteen-of-fifteen reduction for rootless, integrally
   orthogonally decomposable `h=729` endpoint forms; and
2. the exact bare `T20` and `T20 orthogonal_sum LAMBDA24` lattice package.

It does not exclude the surviving `20+24` type, classify rooted or integrally
indecomposable forms, exclude the `h=729` row or `n3=708`, improve
`n3>=708`, construct a compatible endpoint frame or graph, resolve
Conway-99, or establish novelty. All broader statuses remain `UNKNOWN`.
