# Wave 147 orchestrator decision

Date (UTC): 2026-07-28

Decision: **publish scoped checkpoint; continue two-root lane**

Target status: **UNKNOWN**

## Promoted

The public checkpoint may state:

1. the exact bivariate `S6/n3` bridge and rooted seven-vertex profile filter;
2. the independently verified corrections to the Wave 139 additive-`GF(4)`
   model;
3. exact rational feasibility of the complete one-root six-to-seven
   aggregate endpoint relaxation; and
4. construction and independent verification of the finite two-root
   order-eight flag coefficient model.

## Not promoted

The checkpoint must not state:

- a strict bound `n3<4158`;
- existence or nonexistence of `srg(99,14,1,2)`;
- integral code, graph, or lattice realization;
- target dual distance fifteen;
- infeasibility from any HiGHS status;
- feasibility or infeasibility of the unrun two-root endpoint SDP; or
- external novelty or priority.

## Why the strategy changed

Wave 144 showed that each six-set can be made locally feasible.  Wave 146
then glued every six-set to one outside root and the full order-seven deck.
The old endpoint witness failed, but another exact rational endpoint witness
survived.  This localizes the obstruction: it is not one-root consistency but
agreement between two roots on their common eight-vertex data.

Wave 147 supplies precisely that next representation.  Its moment entries
carry `n3` directly, so a rational dual certificate could yield a general
upper bound rather than only exclude one handcrafted endpoint table.

## Next authorized research step

1. Add marked degree and common-neighbor extension rows to the 916
   order-eight classes.
2. Form the unrestricted ordered-edge and ordered-nonedge PSD endpoint
   problem with no graph automorphism assumptions.
3. Run a bounded numerical scout only to discover a candidate primal or
   dual.
4. Rationalize any dual into exact positive-semidefinite Gram blocks plus
   exact count identities.
5. Require independent coefficient and certificate replay before changing
   the rigorous interval.

Until step 5 succeeds, retain

```text
708 <= n3 <= 4158
Conway-99: UNKNOWN
```
