# Independent Wave 21 six-/seven-vertex audit

Verdict: **PASS for the candidate's scoped negative result, with the printed
source equation separately REFUTED and its explicit correction independently
confirmed.**

The pinned six-vertex paper's printed `m7(n-5)` equation is false as written:
it omits `+n23`.  The independent graph-deck reconstruction uniquely forces
that missing card.  After applying only that named correction, the candidate's
62 six-vertex formulas, 19 Hamiltonian seven-vertex formulas, index
alignments, and exact feasibility result all reproduce.

This audit does not change central project status.  It does not resolve
Conway's `srg(99,14,1,2)` problem and does not assess novelty.

## Frozen inputs and independence

Before opening the candidate files, the verifier froze the report and all
eleven attempt artifacts in
`2026-07-23T172537Z-preinspection-freeze.json`.  A post-audit recheck found
`0/12` changed files.

The primary sources were then downloaded independently:

- `https://export.arxiv.org/e-print/2508.03377v2`
  (`f8429d2f839267e2aaf98b04451cb2f0252c0353f75bee6ec6e6947eab0d5834`);
- `https://export.arxiv.org/e-print/2511.06572v1`
  (`10f8d9ea09dc72f4ca6bce4e9427ff1df32718d2978bb35a16db1af3c27cc39a`).

Their top-level TeX hashes exactly matched the submitted pins:

```text
The_Subgraphs_of_Order_Six.tex
823bcaf636a99f6655af453b2a910b9953338db980480572bca81730cfa1b44f

Hamiltonian_Subgraphs_of_Order_Seven.tex
0b06fdc1c2951a344592c6af23abd133c4a8a68d717f199e7a0e2d7ceb909d0a
```

`independent_check.py` does not import or execute candidate code.  It has its
own exact LaTeX arithmetic parser, graph-mask representation,
degree-partition canonicalizer, local-admissibility census, deletion-deck
builder, affine feasibility calculation, and hostile tests.  Candidate JSON
was compared only after the independent result was fixed.

## 1. Independent graph census and index alignment

For each vertex pair, the checker imposes only the necessary local condition
inherited from `srg(n,k,1,2)`:

```text
adjacent pair:    at most 1 common neighbor inside the subgraph
nonadjacent pair: at most 2 common neighbors inside the subgraph
```

It enumerates all labeled masks, canonicalizes under vertex permutations, and
obtains:

| class | count |
|---|---:|
| locally admissible order 4 | 9 |
| locally admissible order 5 | 21 |
| locally admissible order 6 | 62 |
| locally admissible Hamiltonian order 7 | 19 |

For order seven it fixes a labeled `C7`, exhausts every subset of the fourteen
remaining chords, and canonicalizes.  The edge-count distribution is
`7^1, 8^2, 9^7, 10^7, 11^2`.

The published 4-to-5 deck matrix has every column sum equal to five.
Unlabeled row signatures uniquely align all nine source `L_i`; their full deck
vectors then uniquely align all twenty-one source `M_i`.  After the explicit
source correction described below, the 5-to-6 deck vectors uniquely and
bijectively align all sixty-two `N_i`.  The independently obtained `L`, `M`,
and `N` maps agree entry-for-entry with the frozen candidate maps.

The resulting source `N3` representative has canonical mask `5941`, eight
edges, two vertex-disjoint triangles, and two cross edges forming a matching.
This also agrees with the pinned source figure.  Hence the paper's `n3` is
aligned with the project's induced two-triangles/two-independent-cross-edges
count, not an independent-triple count.

No automorphism of a hypothetical target graph is assumed.

## 2. Raw printed failure and unique correction

The primary TeX at the printed 5-to-6 deck table gives only five deletion
cards in the `N23` column:

```text
61 printed columns have coefficient sum 6;
the printed N23 column has coefficient sum 5.
```

Exact target specialization of all printed deck equations gives:

```text
20/21 residuals = 0;
m7(n-5) left-minus-right residual = n23 = 1496880 + 4*n3.
```

The raw result is therefore preserved as `FAIL_AS_PRINTED` in
`independent-raw-printed-results.json`; it is not silently repaired.

Independently, the verifier tried adding the missing single deletion card to
each of the twenty-one possible `M_i` rows.  Only row `M7` produces any actual
locally admissible six-vertex deck.  Adding `+n23` to that row then gives:

```text
all 62 deletion-column sums = 6;
all 62 source N_i map uniquely and bijectively;
all 21 target-specialized 5-to-6 identities have residual 0.
```

Thus the narrow supporting-equation correction

```text
printed m7(n-5) RHS  +  n23
```

is independently confirmed.  This is a source erratum, not a contradiction
in the target parameters.

## 3. All published affine formulas

The independent parser reads the pinned source formula blocks directly.  At
`n=99,k=14` it reconstructs exact rational affine forms for all sixty-two
`n_i` and all nineteen `h_i`.

After independent derivation:

```text
62/62 specialized six-vertex rows match the candidate;
19/19 specialized Hamiltonian seven-vertex rows match the candidate;
9/9 four-to-five source identities have residual 0;
18/18 displayed seven-vertex derivation identities have residual 0;
sum_i n_i = C(99,6) = 1,120,529,256, with n3 coefficient 0.
```

The full row-by-row coefficients are recorded in
`independent-corrected-results.json`; the subsequent comparison is frozen in
`candidate-comparison.json`.

## 4. Exact feasibility

For the sixty-two six-vertex formulas, simultaneous count integrality and
nonnegativity, including `n3>=0`, is exactly:

```text
n3 = 0 (mod 3);
0 <= n3 <= 4158;
n3 in {0,3,6,...,4158}.          1,387 values
```

The upper facet is `n1=1386-n3/3>=0`.  Hostile mutation of that slope changes
the computed facet, so the test suite detects a weakened or mistranscribed
constraint.

For every one of those 1,387 values, the nineteen Hamiltonian formulas are
simultaneously integral and nonnegative exactly when:

```text
h11 = 0 (mod 4);
ceil_to_multiple_of_4(2*n3) <= h11 <= 4*n3.
```

The two active facets are `h16=h11-2*n3` and
`h18=n3-h11/4`.  A hostile denominator mutation in `h18` breaks the recovered
interval and is detected.

Intersecting with the independently audited incumbent `n3>=705` therefore
leaves every value:

```text
n3 = 705,708,...,4158.           1,152 values
```

At `n3=705`, exactly 353 choices remain:

```text
h11 = 1412,1416,...,2820.
```

Consequently, the encoded published count constraints provide no stronger
lower bound, no upper bound below 705, and no contradiction.  This is a
verified negative result about these necessary conditions only; feasible
count vectors are not graph constructions.

## 5. Reproduction and checks

With exact source TeX files extracted from the two pinned archives:

```powershell
.venv\Scripts\python.exe -B verification\wave21-six-vertex-lp\independent_check.py `
  --six-tex <path-to-The_Subgraphs_of_Order_Six.tex> `
  --seven-tex <path-to-Hamiltonian_Subgraphs_of_Order_Seven.tex> `
  --mode raw `
  --output verification\wave21-six-vertex-lp\independent-raw-printed-results.json
# Expected process exit 1 and preserved FAIL_AS_PRINTED.

.venv\Scripts\python.exe -B verification\wave21-six-vertex-lp\independent_check.py `
  --six-tex <path-to-The_Subgraphs_of_Order_Six.tex> `
  --seven-tex <path-to-Hamiltonian_Subgraphs_of_Order_Seven.tex> `
  --mode corrected `
  --output verification\wave21-six-vertex-lp\independent-corrected-results.json

$env:WAVE21_SIX_TEX='<path-to-The_Subgraphs_of_Order_Six.tex>'
$env:WAVE21_SEVEN_TEX='<path-to-Hamiltonian_Subgraphs_of_Order_Seven.tex>'
.venv\Scripts\python.exe -B -m unittest -v `
  verification\wave21-six-vertex-lp\test_independent_check.py
```

Observed:

```text
raw printed run:        expected exit 1, FAIL_AS_PRINTED
corrected run:          exit 0, PASS_EXPLICIT_CORRECTION
hostile tests:          19/19 PASS
raw JSON replay:        byte-identical
corrected JSON replay:  byte-identical
candidate freeze check: 0/12 changed
```

Key artifact hashes:

| artifact | SHA-256 |
|---|---|
| preinspection freeze | `5b5577393b343d876e868f13cd57b80b543e4c61be5f0b84e4a2e8244087d717` |
| independent checker | `d77a7bf134ab73975171245c004ddc0bc9405d428ed35f60c6ae1f004dc92f24` |
| hostile tests | `d99dff439b90d1af4c180397a79bebd1b9de9353f892c3a9255dc856463a7d3d` |
| raw printed result | `50129e12b180c4f7ad7655cb4febe229588a855042a04b28817eeb52a3b27fb6` |
| corrected result | `1c0cc560206ce388308573f2174a0a6189ae909f83a4acb86093063b5adfeacd` |
| candidate comparison | `99222ab7b48d3c888e1f03684a25b771cc52d8a8e9d9faf0b12664b6b919671f` |
| source manifest | `ac44ce9120fdad127177bed6925517dfd59529d836d4ea06c3693441441d6465` |
| test stderr log | `dbdb433bb80cc04c9a88adc8bdfda92d44c8a49f7d7c30250b696a4cc1fefafe` |

## Final disposition

```text
raw printed m7(n-5) equation:             REFUTED
explicit +n23 correction:                 PASS, independently confirmed
4->5 and corrected 5->6 index alignment:  PASS
62 six-vertex specialized formulas:       PASS
19 Hamiltonian seven-vertex formulas:     PASS
encoded exact feasibility exhaustion:     PASS, inconclusive
improvement beyond n3>=705:               NONE
Conway srg(99,14,1,2):                    UNKNOWN
novelty:                                  NOT ASSESSED
```

The orchestrator retains responsibility for any central claim-label change,
publication decision, or wording update.
