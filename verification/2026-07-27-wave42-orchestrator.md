# Wave 42 orchestrator decision

Date: 2026-07-27 UTC

## Decision

Publish Wave 42 as a verified universal characteristic-seven rank theorem:

```text
rank_F7(M)>=27.
```

Also publish two independently verified scoped reductions: the canonical
mask-51739 joint-incidence model and the branch-15 seventh-triangle clause
family. Do not present any of these as a Conway graph, counterexample,
endpoint exclusion, upper bound below `n3=4158`, or novelty/priority result.

## Why the universal theorem is promoted

The rank discovery and verifier use separate implementations. The verifier
froze its inputs and method before comparison, regenerated all eleven local
types, explicitly checked 1,714,426,560 even-type labelled pairs, and used an
independent pivot/mate CSP covering 19,916,886,528,000 all-odd labelled pairs.
There are zero rank-at-most-one residuals.

Discovery and verification agree on all theorem fields and all seven complete
labelled minimum-border permutation streams. Exact rank-one positive controls,
rank-two hostile controls, zero-diagonal hostile controls, coverage mutations,
and status-inflation tests pass.

The verifier's first byte replay correctly exposed serialized wall-clock
timings. The failed freeze is retained; the repair removed only timing
metadata. The deterministic result then passed a complete fresh replay.

## Promoted theorem

Every edge-local 39-point block has characteristic-seven rank at least 27.
Since it is principal in the transported global matrix and rank transport was
verified in earlier waves, every hypothetical target satisfies

```text
rank_F7(M)>=27.
```

This theorem assumes neither `n3=4158` nor a graph automorphism. At the
prism-free endpoint it reduces the arithmetic rank-pair census from 314 to
297; if `r3=12`, parity forces even `r7>=28`. The rank ceiling 44 remains
compatible.

## Scoped reductions

The canonical mask-51739 verifier independently reduces one conditional
outside-incidence problem to 45,032 necessary six-set candidates and derives
exact overlap and outside-graph counts. It also produces a second, distinct
two-fibre positive certificate. No full three-fibre `B` or outside graph `H`
is constructed or excluded.

The branch-15 verifier independently derives a seventh fixed triangle and
matches all 64,932 raw and 33,778 active prism clauses. No unit or
contradiction follows.

## Continuation

The best next targets are:

1. solve the exact 45,032-candidate three-way matching for canonical mask
   `51739`, then impose the full outside graph equations;
2. generalize the component-equality argument across all 264 rank-33 lifts
   and then the remaining triangle-free lifts;
3. add proof-producing cuts involving two unfixed triangles to branch 15 and
   the other 32 cases; or
4. turn rank-27 equality obstructions into a higher-order global motif
   inequality forcing at least one prism.

## Publication classification

```text
universal rank_F7(M)>=27:              VERIFIED
conditional endpoint rank pairs:       297
canonical mask-51739 B/H reduction:     VERIFIED SCOPED
branch-15 seventh-triangle reduction:   VERIFIED SCOPED
endpoint proof coverage:                0/33
upper bound below n3<=4158:             NOT PROVED
target, novelty, and priority:          UNKNOWN
```
