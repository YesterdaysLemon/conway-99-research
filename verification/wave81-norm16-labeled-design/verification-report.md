# Wave 81 verification report

Verdict: **VERIFIED**, conditional on the frozen Wave 78 norm-16 signed
support branch.

An independent checker confirms the complete 1,800-matrix anchored support
census and its reduction to five `S8 x S8` label orbits without a graph
automorphism assumption. It reconstructs the unique outside histogram
`(11,64,8)`, all 4,985 deficiency-pair coupling multisets, and all 4,985
positive per-`X2` marginal-flow results. A hostile zero-capacity control
fails as required.

The local type equations, six outside pair-moment identities, and complete
histogram sets match the discovery artifact exactly: 43 rows at `t=0`,
seven at `t=1`, and none for `t>=2`. The corrected Jacobi factors use full
SRG multiplicities `3^54` and `(-4)^44`, producing principal exponents
`38,28`; the swapped formula gives the impossible trace `-70`. All five
residual characteristic polynomials, determinants, traces, and four-cycle
counts match. No support orbit is eliminated.

The verifier additionally derives a stricter degree-graphicality filter:
three `t=0` rows have nongraphical `X0` induced degree sequences, leaving
40 plus seven degree-level rows. This extension is explicitly `DERIVED`
and requires a second verifier before promotion.

No simultaneous 83-vertex outside graph has been constructed. The norm-16
branch remains live. Conway-99 and literature novelty remain `UNKNOWN`.
