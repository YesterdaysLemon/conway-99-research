# Verification protocol

1. Freeze the endpoint integer reflection and centered Gram.
2. Derive pairwise support orthogonality directly from the kernel equations.
3. Treat the two coefficient orbits separately.
4. Use an integer lift different from the discovery norm count for the mixed
   orbit.
5. Recover the all-plus joint distribution from pairwise row-distance energy
   and row margins.
6. Audit which conclusions require `P=0` and which do not require `r3=12`.
7. Retain the target as `UNKNOWN`.
