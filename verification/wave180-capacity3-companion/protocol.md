# Verification protocol

1. Treat every Wave 180 assertion and program result as untrusted.
2. Validate all frozen inputs and discovery package entries.
3. Derive the multiplicity-three star shape from exact transversals.
4. Reconstruct the seven `j` counts using only `lambda=1` and `mu=2`.
5. Apply the star projector only after proving `z_T` lies in `E_x`.
6. Reject `t=0` through the exact duplicate direction.
7. Derive both `t=3` circuit relations and verify the switched conic Gram.
8. Prove the companion has exactly the same three labels and defines a
   fixed-point-free involution.
9. Use an inclusion-minimal nonedge cover before counting companions.
10. Count projective classes before multiplying by two ternary scalars.
11. Perform no graph, code, SAT, configuration, or isomorphism search.
12. Preserve endpoint status as `UNKNOWN`.

