# Wave 66 independent spherical/lattice audit

Date UTC: 2026-07-27T23:05:00Z

Verdict: **VERIFIED_WITH_CORRECTION**.

The conditional mathematical theorem passes.  Three discovery statements
require correction, none of which changes the theorem's final eight
surviving ranks.

```text
even rank-44 difference lattice M:                 VERIFIED_SCOPED
63 M* subset M:                                    VERIFIED_SCOPED
det(M)=9*7^(44-r):                                 VERIFIED_SCOPED
M*/M = Z/9 direct_sum (Z/7)^(44-r):               VERIFIED_SCOPED
min(M*)>=2:                                        VERIFIED_SCOPED
Milgram: r even and r<=42:                         VERIFIED_SCOPED
universal surviving ranks:                         28,30,32,34,36,38,40,42
Blichfeldt consequence:                            only r>=18
graph existence / Conway-99 / novelty:             UNKNOWN / UNKNOWN / UNKNOWN
```

Here

```text
S=2A-J+I,   r=rank_F7(S),
```

and every claim is conditional on a hypothetical
`srg(99,14,1,2)`.  No automorphism or endpoint hypothesis enters the newly
verified lattice theorem.

## Three recorded corrections

First, the discovery package calls `28<=r<=44` the imported verified interval
under its unqualified hypothetical-graph scope.  The prior universal theorem
is instead

```text
27<=r<=44.
```

The prior `r>=28` theorem is conditional on `n3=4158`.  Wave 66's new
Milgram parity removes universal rank 27, so the corrected universal
conclusion is still exactly

```text
r in {28,30,32,34,36,38,40,42}.
```

Thus Wave 66 upgrades the old universal lower bound from 27 to 28 while also
giving the universal upper bound 42 and parity restriction.  This is
new-to-project; literature novelty and priority were not established and
remain `UNKNOWN`.

Second, discovery's hostile-controls file says that all three imported Wave 2
weight-eight facts are used and that dropping any one invalidates the
dual-minimum proof.  The proof actually uses:

- minimum weight at least eight in `ker_F2(A)`; and
- independence of a weight-eight support.

The separately verified statement that every graph vertex meets a
weight-eight support in zero or two vertices is not used in any Wave 66
norm exclusion.

Third, the discovery JSON limitation sentence says that all 17 imported rank
rows survive.  All 17 endpoint rows enter its Milgram check, but only eight
survive.  The discovery's main survivor table and count are correct.

Discovery files were not edited.

## Independent reconstruction

### Spherical embedding and centering

From

```text
A^2=12I-A+2J,   AJ=14J,
```

the primitive idempotents are

```text
E_3  = (A+4I-(2/11)J)/7,
E_-4 = (-A+3I+(1/9)J)/7.
```

Their normalized adjacent/nonadjacent inner products independently reduce to

```text
dimension 54:  3/14, -1/21
dimension 44: -2/7,   1/28.
```

Adding a constant coordinate with squared weight `1/9` to the second
embedding gives inner products `-1/7,+1/7`.  Scaling by `sqrt(7)` gives

```text
B=6I+J-2A=7I-S
```

with spectrum

```text
77^1, 0^54, 14^44.
```

The Seidel convention is therefore consistent:

```text
S spectrum = -70^1, 7^54, (-7)^44.
```

Centering and dividing pairings by two gives

```text
C=3I-A+J/9
```

with spectrum `0^55,7^44`.  The constant-coordinate direction is removed,
so the resulting lattice has rank 44, not 45.  For zero-sum integer
coefficient vectors its form is `3I-A`; it is integral and even.  The
frame operator is exactly `7I`.

### Saturation and determinant

Let `L` be generated by the lifted Gram vectors and `D` by their
differences.  Every rational relation lies in the `A`-eigenvalue-three
space, hence has coefficient sum zero.  Therefore the kernel of
`Z^99 -> L` lies inside the zero-sum coefficient lattice, and

```text
L/D is infinite cyclic with no torsion.
```

This proves the saturation needed for the height calculation.  The
projection of one generator to `D`'s orthogonal line has squared height
`7/9`, while `D` carries twice the form of `M`; hence

```text
det(L)=(7/9)*2^44*det(M).
```

Because the 99 vectors generate `L`, `det(L)` is the top determinantal
divisor of their Gram matrix.  Modulo two, `B=J` has rank one, so at least 44
powers of two divide that divisor.  The nonzero pseudodeterminant

```text
77*14^44 = 2^44*7^45*11
```

contains exactly 44 powers of two, forcing equality.

For `y in M*`, put `t_i=<y,u_i>`.  The differences `t_i-t_j` are integral,
and centering gives

```text
t_i=n_i+a/99,   sum n_i=-a.
```

The `-4` eigenvector equation gives

```text
(A+4I)n=-(2a/11)1,
```

so `11|a`.  Writing `a=11b`, the frame identity first gives
`7y=sum n_i u_i`; the coefficients `9n_i+b` have total

```text
9(-11b)+99b=0.
```

Thus `63y` is in `M` and

```text
63 M* subset M.
```

This excludes an 11-primary discriminant factor and bounds the 3- and
7-primary exponents by 9 and 7.

Modulo three, the radical of the form on the zero-sum coefficient space is
`ker(A)+<1>`, of dimension 55.  Quotienting by the saturated
54-dimensional rational relation lattice leaves rank 43.  Hence there is
one 3-primary invariant factor; its valuation is two, so it is `Z/9`.

Modulo seven, the zero-sum space complements `<1>`, and on it

```text
2(3I-A)=B=-S.
```

After quotienting by the relation lattice, the form rank is exactly `r`.
The exponent bound makes each nontrivial 7-primary factor elementary, giving
exactly `44-r` copies of `Z/7`.  Therefore

```text
det(M)=9*7^(44-r),
M*/M = Z/9 direct_sum (Z/7)^(44-r).
```

On every surviving row the group exponent is 63.  Since 63 is odd and `M`
is even, `63(x,x)` is even for every `x in M*`; nondegeneracy also forces
the level to be a multiple of the group exponent.  The exact lattice level
is therefore 63.

### Dual minimum

The frame identity gives

```text
7||y||^2=sum t_i^2.
```

The common fractional numerator is a multiple of 11.  For numerators
`22,33,...,77`, the unconstrained minimum energy is at least `154/9>14`.
Numerators 11 and 88 are negatives of one another.  At numerator 11, the
integer equation forces positive mass at least two and therefore
`sum n_i^2>=15`.  Equality would consist of two `+1` entries and thirteen
`-1` entries.  Every negative coordinate would have to see both positive
coordinates and no negative coordinate, forcing each positive coordinate
to see all thirteen negatives, incompatible with its required neighbor sum
`-6`.  Parity then raises the integer squared norm to at least 17, so

```text
sum t_i^2 >= 17-11/9 = 142/9 > 14.
```

In the integral coset, divide by a common power of two.  The primitive
vector's odd support is a nonzero word of `ker_F2(A)` and has size at least
eight.  Exact magnitude-profile enumeration leaves only:

```text
norm  8: eight entries of magnitude 1
norm 10: ten entries of magnitude 1
norm 12: twelve entries of magnitude 1
         or eight entries of magnitude 1 and one of magnitude 2.
```

The imported independence of a weight-eight support excludes both
weight-eight cases.  For norm 10, the spectral induced-edge bound is
`185/9`, hence at most 20 edges.  The eigenvector equations then force a
4-regular bipartite graph on `5+5`, namely `K5,5` minus a matching; two
same-side vertices already have three common neighbors, contradicting
`mu=2`.

For norm 12, the spectral bound is 26 edges and again forces the support to
be 4-regular bipartite, now on `6+6`.  One part contributes

```text
6*C(4,2)=36
```

common-neighbor incidences, while its 15 nonadjacent pairs permit at most
`15*mu=30`.  This is impossible.  Hence `sum t_i^2>=14` in every nonzero
dual coset and

```text
min(M*)>=2.
```

Equality remains possible only through an integral `-4` eigenvector of
squared norm 14.

### Finite quadratic forms and Milgram

For every nondegenerate form on `Z/9`, direct cyclotomic residue counts give
normalized quadratic Gauss phase `+1`.  A `q`-dimensional elementary
7-primary form diagonalizes over `F_7`; each one-dimensional factor has
phase `+i` or `-i`, so the total phase is

```text
delta*i^q,   delta in {+1,-1}.
```

The positive rank-44 lattice has Milgram phase

```text
exp(2*pi*i*44/8)=-1.
```

Therefore `q=44-r` must be even.  The case `q=0` is separately impossible:
the absent 7-primary part has fixed phase `+1`, not an adjustable sign.
Thus `q` is positive and even, proving

```text
r even,   r<=42.
```

Combining this with the imported universal `27<=r<=44` gives exactly the
eight universal ranks listed above.  For every positive even `q`, one of the
two 7-primary determinant signs has the required phase, so this group-level
test has no further exclusion.

### Blichfeldt cross-check

The standard dimension-44 Blichfeldt bound and `min(M*)>=2` imply

```text
det(M)<=23!^2/pi^44.
```

Exact rational bounds on pi exclude rank 17 but retain rank 18.  This route
therefore yields only `r>=18`, strictly weaker than the prior universal
`r>=27`.

## Independence, replay, and hostile controls

The verifier froze all 13 discovery files, including both hidden bytecode
files, by path, size, and SHA-256 before inspection.  A post-run comparison
found the discovery directory unchanged.

`independent_verify.py` does not import or execute discovery code.  It
reconstructs the Bose-Mesner arithmetic, local Smith profiles, exact Gauss
residue patterns, low-norm magnitude profiles, spectral bounds, and
Blichfeldt threshold.  Only after producing its own result does it read the
discovery JSON for comparison.

The eight compared mathematical fields have zero mismatches.  Hostile
controls reject:

- reversed Seidel multiplicities;
- retaining rank 45 after centering;
- omitting the factor of two;
- a hidden square saturation index;
- `Z/3 direct_sum Z/3` in place of `Z/9`;
- an adjustable phase for the absent 7-primary part; and
- failure to exclude the numerator-11 norm-15 equality profile.

Exact commands:

```powershell
.\.venv\Scripts\python.exe `
  verification\wave66-spherical-code-shift\independent_verify.py `
  --output verification\wave66-spherical-code-shift\independent-results.json `
  --discovery-results attempts\wave66-spherical-code-shift\exact-results.json `
  --comparison-output verification\wave66-spherical-code-shift\comparison.json

.\.venv\Scripts\python.exe `
  verification\wave66-spherical-code-shift\independent_verify.py `
  --verify `
  --discovery-results attempts\wave66-spherical-code-shift\exact-results.json `
  --comparison-output verification\wave66-spherical-code-shift\comparison.json

.\.venv\Scripts\python.exe -m unittest -v `
  verification\wave66-spherical-code-shift\test_independent_verify.py

.\.venv\Scripts\python.exe `
  attempts\wave66-spherical-code-shift\exact_check.py --verify

.\.venv\Scripts\python.exe -m unittest -v `
  attempts\wave66-spherical-code-shift\test_exact_check.py
```

Results:

```text
independent deterministic replay: PASS
independent tests:                14 passed
discovery canonical replay:       PASS
discovery tests inspected:        10 passed
mathematical comparison:           8 fields, 0 mismatches
free physical memory:             above 51%
```

## Boundary

No graph, equiangular configuration, even lattice, norm-14 vector, or
nonexistence certificate is constructed.  The eight discriminant rows are
necessary arithmetic possibilities, not realizations.  Conway-99 remains
`UNKNOWN`; endpoint proof coverage is unchanged; novelty remains `UNKNOWN`.
