# Wave 34 orchestrator correction and nonpromotion ledger

Date: 2026-07-24

Global result before and after every entry:

```text
Conway-99 existence/nonexistence: UNKNOWN
n3=708:                           UNKNOWN
novelty and priority:             UNKNOWN
```

This ledger preserves failed checks, scope repairs, and publication
corrections. It does not rewrite discovery history or turn finite evidence
into a target theorem.

## Rooted structural lane

The verifier resolved the candidate's coordinate, SNF, census-independence,
projector-converse, and modular objections by exact reconstruction. The
substantive publication qualifier is:

- `574,118,037` counts labeled single binary columns satisfying only
  `Pb=2*1`;
- `448,879,368` counts the necessary duplicate-free subdomain after the
  full criterion forbids doubled-support pairs; and
- neither number counts compatible 15-column designs, projectors, or graphs.

The pair-census crosscheck independently reproduced the nine pair states and
the duplicate-free count. No binary solution or exclusion was found.

## Rooted encoding lane

Three verifier corrections are retained without changing a formula byte:

1. The candidate's post-freeze correction ledger is outside both candidate
   manifests. The 18-entry local-manifest description is exact for the
   earlier freeze but not literally comprehensive after that ledger was
   added.
2. The frozen preflight predates later `contiguous` metadata. The published
   generator reproduces all normalized semantic counts but not those frozen
   preflight bytes.
3. The decoder requires a complete assignment of the 5,950 primary
   variables, not every auxiliary DIMACS variable, and does not itself
   evaluate the CNF. A putative model must still pass the independent graph
   checker.

The verifier independently regenerated all 4,323,943 clauses and matched the
89,546,779-byte raw CNF. No solver or proof checker was run; SAT and UNSAT
remain `UNKNOWN`.

## Rootless actual-incidence lane

The candidate reported the wrong SHA-256 for the frozen Wave 33 rootless
comparison result:

```text
claimed:
8b7b27fd67f12bb82c60eb27c72a62c913d06f2d86f3eb626dcddd6ac75afeb

actual:
8b7b27fd67f12bb82cb6eebf645d0c46324d7227e51bdeb0093fbcdf9d753872
```

This provenance claim is `REFUTED` and remains visible. The Stage 2 verifier
used the correctly frozen input and independently reconstructed the scoped
mathematics, so the typo is nonblocking for those derivations.

Two scope repairs are also retained:

- among the six completion vertices, nine pairs are already forced
  nonadjacent and only six pairs remain edge-undecided; and
- the emitted 45-vertex object is partial, not a target graph. Read as a
  complete graph it has 42 degree failures, 24 lambda failures, and 713 mu
  failures.

The new `6,860`-wedge, `3,041`-four-cycle, and
`tr(A_R3^4)>=67,848` consequences are exact scoped derivations, not motif
forcing or endpoint resolution.

## Harrison external-source lane

The first verifier result serialized wall-clock time. A second identical
mathematical run therefore produced a different file hash. The failed result
hash is retained in
`verification/wave34-external-source-audit/harrison/reproducibility-correction.md`.
The repaired result omits timing and local temporary paths; two isolated
runs produced the same corrected result hash.

The first repaired CSV emission also inherited host newline behavior. The
writer was fixed to use explicit LF line endings, and the full manifest was
regenerated. Neither repair changed a recipe hash, certificate-availability
finding, or mathematical verdict.

The verified honest rooted bridge and 1,302 formula recipe identities do not
substitute for the absent or unpulled theorem-ladder proof bodies. R230,
`k>=14`, `k=13`, and every resulting graph exclusion remain `UNKNOWN`.

## Kuber and Selub external-source lane

An initial archive-populated Kuber build route failed because Lake expected
Git metadata while reconciling package URLs. That failed route was retained
and not used as build evidence. A fresh detached Git clone at the pinned
commit then passed cache retrieval and `lake --wfail build`.

Publication QA caught an ignored `lean-build.log` referenced by the manifest
but omitted from the first Git commit. A follow-up commit force-added that
compact evidence summary. A later verifier pass:

- added SHA-256 values to run-report outputs;
- made tests check all frozen inputs, all manifest entries, seven printed
  axiom summaries, 3,068 build jobs, and the cache summary;
- narrowed the trust scan to the audited
  `Conway99C3Orbits/Basic.lean` file; and
- described the retained command records as curated summaries, not full raw
  transcripts.

Selub's displayed quadrilateral formula has a visible indexing gap, but the
audit does not retain a complete Boolean counterassignment against every
other printed clause. Its exact label was therefore tightened from
`REFUTED AS PRINTED` to `NOT VERIFIED AS PRINTED`.

Kuber's clean build verifies a conditional matrix/arithmetic theorem only;
the graph-to-matrix bridge remains unformalized.

## Central-integration chronology

The rooted structural comparison verifier freezes the pre-integration
`STATUS.yaml` byte. After the central Wave 34 status block was added, a
direct full-census run correctly stopped before its tests with:

```text
hash drift for STATUS.yaml:
b2224b3c71e8b5d578bd2571584ca71f40c2f8e2856b5834a8e6f7d1edf0b0f2
!=
feda17934162602804015e17130c029ffdba7bf0307cdf234f4a7d8979298864
```

The first hash is the initial integration-draft byte and is preserved only
as historical failure evidence. The frozen hash remains authoritative.

No verifier hash was weakened. The accepted chronology procedure exports the
exact integrated Git tree to a temporary directory, substitutes only
`STATUS.yaml` from base commit
`0fa5b8161baf8b2a5404a67051b7d61cbc906da3`, verifies its frozen hash, and
runs the unchanged 11-test comparison suite with the full census enabled.
The procedure and the retained failure are in
`verification/wave34-integration-chronology/`.

The first wrapper invocation used an invalid guessed expansion of the short
integration commit. `git rev-parse` rejected it before creating an archive or
running a test. The next invocation passed all 11 tests but its draft JSON
serialized the host's absolute Python executable path. That draft, hash
`ef7eed35236fe5b75696564d85a08d7f6f95826f2dabdbb535c71564158c8aa1`,
is not published. The serializer now emits a portable command, a regression
test enforces that boundary, and the complete replay was rerun. The accepted
result hash is
`b18a3e402a23f39924dd282482a64404443b8aaa2f1fbfcfb32cec09a2d59cb4`.

## Full-range publication-format repair

The first detached finalization replay exposed deliberate Markdown hard-break
spaces in three source-audit reports and one extra terminal blank line in the
rootless Stage 1 failed-routes ledger. The first proposed repair added narrow
`.gitattributes` whitespace rules. The rooted encoding verifier rejected that
proposal because `.gitattributes` is itself a frozen formula-packaging input.
The failed proposal was reverted; no verifier input hash was relaxed.

The four prose files were normalized instead. Every affected run report and
manifest was rebuilt through the complete provenance chain. The resulting
rootless Stage 1 and Stage 2 manifest hashes are
`27c13fb233af677c1619d571bf73820cddd4451172fdd749ace1c9f65f8f3c8d`
and
`74d1aa2a95b3ce13a47a19fddafd6e78fec193a20fd92c035e2c872345c795bc`.
The Harrison, status/design, and consolidated external manifest hashes are
`6052d3114a89af378cf412cc3126b60bab9dc9fe249a673188d3369a681db386`,
`1af4bad56405fb24128285fa586e2d1e2b80c82f1bdffdafa7cbdb6053cd6812`,
and
`f21ec00738093ac6579654c88bff3a0c9b35665c2dee83cd5cd9422b9eab483c`.

The rootless comparison was regenerated from the repaired frozen inputs and
matched its updated accepted result at
`0ba02392424f1fb8507c18c253d423834cb27e6ff945b258d15e757037e1561a`.
Only provenance hashes changed; every mathematical field and every global
`UNKNOWN` wall remained unchanged.

## Current-source lane

Brouwer's maintained `?` marker, McKay's contextual approximate
`1.5 x 10^21` design scale, and the Rijeka parent/derived corpus counts pass
as scoped source characterizations. They are not proofs of global openness,
an exact design enumeration, or an unrestricted complete catalogue.

## Final publication wall

Wave 34 verifies an unrestricted rooted encoding, a strict rooted structural
reparameterization and necessary single-column reduction, several rootless
actual-incidence lemmas and moment bounds, and precisely scoped external
source facts. It supplies no graph, no complete-domain UNSAT certificate, no
rooted binary solution, no rootless mixed-motif forcing theorem, no endpoint
resolution, no `n3=708` exclusion, and no novelty or priority result.
