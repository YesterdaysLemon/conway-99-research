# Retained limits and failed routes

1. **Retained \(Q_2\).** Exact residual \(40+40=80\); it is not a complete
   third group.
2. **Fixed-\(Q_1\) `UNSAT`.** No proof artifact was exported. The solver
   status is diagnostic only and cannot be promoted.
3. **Generalizing fixed \(Q_1\).** Invalid. Even a certified refutation of
   this \(Q_1\) would leave other first-stage permutations open.
4. **Unrestricted residual 108.** No associated permutations are stored, so
   the score cannot be independently replayed.
5. **Entering the \(D\) layer.** Premature without a complete exact
   \(36\times60\) factor \(C\).

The next honest target is either an exact third-group extension for some
\(Q_1\), or a proof-producing universal obstruction over all admissible
first-stage permutations.
