# Wave 177 independent verification

Status: `VERIFIED_WITH_SCOPE`.

The adjacent and nonadjacent true-relation-code averages, resulting support
bounds, coefficient-sum lift, and the type `4+2` conic/frame calculation
were independently reproduced.

The theorem gives a relation indexed by each vertex pair.  It does not show
that relations indexed by different pairs are distinct.  The local conic is
a null control for the frame equation, not an endpoint configuration.

