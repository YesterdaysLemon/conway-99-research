# Verification protocol

1. Treat the Wave 177 prose, archived JSON, and discovery program as
   untrusted.
2. Verify the discovery manifest and every frozen input before relying on
   Wave 176 or Wave 174.
3. Average over actual coefficient-relation codes, never over a Gram kernel.
4. Prove full support from the displayed star subcode before applying the
   coordinate-counting identity.
5. Identify exactly the intersection of the individual-star circuit span
   with the common-coordinate-zero code.
6. Use dual distance four only after proving that an outside word is a true
   cross-star relation.
7. Reconstruct the type `4+2` weight-four word from its actual 13-column
   Gram matrix and verify the switched Gram before using conic geometry.
8. Treat the complete plane frame as a local null control only.
9. Distinguish 693 edge-indexed choices from 693 distinct dual words.
10. Perform no graph, code, SAT, or isomorphism search.

