# Source-contact log

The blind protocol was fully frozen before any event below.

## Accidental one-line match before independent code was complete

A terminology search excluded `attempts/wave209-rank3-trade-proof-a/**` but
did not exclude the separately stored sealed agent report.  `rg` returned one
line from `agents/2026-07-31-wave209-rank3-trade-proof-a.md` stating that an
imported 22-vertex `x=6` control remains a hostile local witness.  The report
was not otherwise opened.  No formula, algorithm, result table, or certificate
was exposed.

This contact is retained rather than hidden.  The same local control is part
of the already-public Wave 208 replay surface, and the independent Wave 209
enumerator will derive its domains and counts without reading or importing the
sealed report or discovery code.  Post-source comparison will still be kept
as a separate phase, but the audit must qualify the pre-source reconstruction
as having this limited one-line metadata exposure.
