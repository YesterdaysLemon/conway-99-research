# Wave198 clean-room protocol

Frozen before opening either Wave198 source package.

1. Use only sealed Wave180, Wave194, Wave196, and Wave197 verifier results.
2. For the selected exact-three flag hypergraph, count oriented label
   multiplicities and prove `m(x->y)<=5`.
3. Orient each selected flag label by its canonical center.  Prove that
   every selected type-three private label contributes one distinct
   degree-one orientation.
4. If `T` is the selected oriented-label union, derive
   `3n3<=5T-4p3`.
5. Audit separation `T+a3+b3<=J` and combine it with `J<=3564` to obtain
   `S5=17820-3n3-4p3-5a3-5b3>=0`.
6. Retain `SH=3h-a3-b3>=0`, `SF=1287-n3-h-g>=0`, and the verified
   Wave194 slacks.
7. Reconstruct the exact certificate target
   `(76C-349V)/40=281457/40`; check integer rounding `Q>=7037` and all
   projective/scalar totals.
8. Seek an integer arithmetic control for the ceiling and label it as
   accounting only.
9. Freeze the independent result before opening proof B or hostile proof A.
10. Preserve rank 11, endpoint existence, and Conway-99 as `UNKNOWN`.

No graph, configuration, cover, SAT, LP, construction, enumeration,
isomorphism, or brute-force search is used in the mathematical derivation.
