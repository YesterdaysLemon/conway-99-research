# Wave200 independent verifier protocol

1. Freeze the exact claim: under the prism-free rank-11 endpoint, exclude
   only the integer faces `Q0=7037,7038` and infer `Q>=7039`.
2. Use only sealed Wave196 and Wave198 verifier theorems for the local
   four-fibre geometry, the full-pool cap, and the orientation row.
3. Reconstruct `S5=5*delta+5*eta+epsilon` and the exact `q` identity from
   definitions, without importing Wave200 discovery code.
4. Check the coefficientwise domination that forces `s>=27` when
   `B<=63`.
5. Attack the local additivity step.  In particular, justify why two
   multiplicity-five orientations cannot occupy the same block-pair
   fibre.
6. Instantiate only the two cited Hilton--Milner equality formulas; do
   not search intersecting families.
7. Reconstruct the global upper bound `s<=4`, the contradiction, integer
   rounding, and projective/scalar totals.
8. Replay the sealed source and hostile proof-agent packages only after
   the independent derivation is fixed.
9. Preserve rank 11, endpoint existence, strict original `n3`
   improvement, external novelty, and Conway-99 as `UNKNOWN`.

No graph, flag-family, code, cover, SAT, LP, configuration, enumeration,
isomorphism, or brute-force search is part of the mathematical audit.
