# Independently confirmed surviving and failed routes

## Gram PSD does not exclude the endpoint

`D+5I=ZZ^T` and `R+3I=Z^TZ` force least-eigenvalue and nullity conditions,
but the exact local positive control satisfies them. These are necessary
conditions only.

## Four-cycle transfer is nonempty

The exact identity `c4(R)=1260+c4(D)` narrows a target to
`1260<=c4(R)<=2331`. The interval is nonempty, so this route alone gives no
contradiction.

## Finite-field ranks do not exclude

Modulo 3, `Z^T 1=0` forces `rank_3(Z)<=83`. The positive control attains 83,
while its ranks modulo 2, 5, and 7 are all 84. These ranks calibrate the
necessary conditions but do not exclude the endpoint.

## Scalar projector compression survives

The exact interval `64/5<=tr(T E_3)<=16` is nonempty. The scalar assignment
`w=14` satisfies all projector-trace bounds and the mixed-moment identity.
It is not a matrix realization.

## All one-root scaffold averages survive

For all 43 integer values `y=0,...,42`, all six exact rational eigenvalue
lanes of the averaged `D+5I` are nonnegative. The minimum is `12/5`.
Group averaging is necessary only and does not assume target symmetry.

## Local positive control deliberately misses the target

The unlabelled hypergraph and transition control satisfies all local
decomposition conditions but differs from target `B` moments by
`0,0,0,+5496,-12020,+239772`. It is not a target candidate.

## Remaining route

The first untested layer is the noncommutative two-root placement of the
140 columns of `Z` and the transition factor `T` in the fixed signed-edge
scaffold, with the entrywise residual equation enforced. Endpoint,
Conway-99, strict upper-bound improvement, and novelty remain `UNKNOWN`.
