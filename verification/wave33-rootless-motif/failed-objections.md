# Wave 33 rootless-motif adversarial objections

## Sustained

### The phrase "all two-leg contractions" is broader than the proof

The exact identity proves closure for `N^T p(A) N` and, by multiplication, for
matrix words that reduce to that form.  It does not classify every statistic
that could informally be called two-leg.  In particular it does not cover
uncontracted three-leg data, individual `R2` or `R3` indicators, or full
projector/Schur compatibility.

Disposition: nonblocking wording correction.  The audit supplies the scoped
replacement.  The candidate's own limitations already preserve the correct
mathematical boundary.

### Local controls might not extend globally

The formal tables and partial 99-vertex edge assignments need not extend to a
single global triangle relation tensor or to an `srg(99,14,1,2)`.

Disposition: valid and blocking for every global conclusion.  This is why
global motif forcing, motif avoidance, the rootless endpoint, `n3=708`,
Conway-99, and novelty remain `UNKNOWN`.

## Rejected by exact checks

### The submitted tables violate feasibility

Rejected.  Both tables are symmetric nonnegative integer tables with the
required `q=2` margins and fixed `R2` cells.  Hostile Boolean entries, margin
changes, fixed-cell changes, and contraction changes are all rejected.

### The null trade is visible to the fused algebra

Rejected.  Its row and column margins vanish, and all sixteen ordered
`{I,J,Gamma,C}` bilinear contractions vanish.  The decisive `R3,R3` entry
changes by one.

### The incidence shift is `-3`, not `-4`

Rejected.  Independent spectral evaluation verifies
`N^T A^k N=(Gamma+3I)(Gamma-4I)^k`; a hostile `-3` mutation fails.

### The multiplicity board or the four candidates were copied from JSON

Rejected.  The verifier derives the board from `lambda=1`, `mu=2`, degree
`14`, and the two normalized cross edges, constructs all weighted board
vertices, and independently enumerates the four transversals.  Mutating a
cell, vertex, relation label, or transversal fails.

### The partial controls are complete graph constructions

Rejected.  The verifier reconstructs only the fixed edges and checks exact
scope text in both JSON and prose.  Mutations claiming completeness,
extendibility, or a global graph fail.

### The trace counts each unordered motif once

Rejected.  A direct three-object matrix witness gives trace `2` for one
unordered motif, one contribution for each orientation of the `R2` pair.

### A local result was promoted to a global result

Rejected.  Exact schemas and an explicit status wall reject changed or added
`VERIFIED` global claims, altered blocking objections, automorphism
restrictions, or claims that a global graph was constructed.
