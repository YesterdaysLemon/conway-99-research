---
role: verifier
date_utc: 2026-07-23T20:21:56Z
git_commit: c82f49be256f79fed45b7a4f1458d751d2ec0d9f
claim_label: VERIFIED
scope: exact feasibility of the encoded orbit-refined one-vertex extension relaxation through order seven at n3=705 for every allowed h11
inputs:
  - path: verification/wave23-weighted-extensions/preinspection-freeze.sha256
    sha256: b100e042a7e1ebba796410b87e592a60dcd26ba85463ff9f14a3634e6afaa427
  - path: attempts/wave23-weighted-extensions/affine-witness.json
    sha256: 16ade54a77589b91478b3426dc3dbe57ebb2a5e0f7d209602ca282e42ac3ec77
  - path: attempts/wave21-six-vertex-lp/exact-results.json
    sha256: 5e7b6f526985fb719754145944579aacb0e8f38e9e14a54d2075552a3756ff2b
  - path: attempts/wave22-full-seven-deck/exact-results.json
    sha256: ca5d9d116f6a9d6e355600429652e2bf4474b73dcf281bbcb564420d820acbd2
method: independent complete graph census, automorphism-orbit reconstruction, exact integer replay, modular-rank certificates, source-gate reconstruction, and hostile mutation tests
command: python verification/wave23-weighted-extensions/independent_verifier.py --output verification/wave23-weighted-extensions/independent-results.json
outputs:
  - path: verification/wave23-weighted-extensions/independent_verifier.py
    sha256: 1ea9e69646676016afb9b00494f60724d1e7f35fc8654994398b7c8008ad4a51
  - path: verification/wave23-weighted-extensions/test_independent_verifier.py
    sha256: 95bc1ec8d1520b385d3d06ec209dd0a4de6ccb7156c15516a57322ebaec366aa
  - path: verification/wave23-weighted-extensions/independent-results.json
    sha256: fecd813402ddb3aabd2434833f020edaf11e82c4392fc6ecc7d1f300b7d97eb4
limitations: verifies a necessary aggregate counting relaxation only; no overlap consistency or 99-vertex adjacency matrix; Hamiltonian panel alignment remains a cited input
---

# Independent audit: Wave 23 weighted extensions

## Verdict

`VERIFIED`, strictly within the encoded relaxation.

I independently reproduced the complete orbit-refined one-vertex extension
system and accepted the submitted affine integer family.  The system is
exactly feasible at `n3=705` for every

```text
h11 = 4z,  z = 353,354,...,705.
```

No mathematical or packaging defect was found in the frozen discovery
artifacts.  This result does **not** construct an SRG, improve the lower bound,
or change the target-existence status from `UNKNOWN`.

## Separation and freeze

Before inspecting any discovery internals, I SHA-256-froze the discovery
report and all nine files in its attempt package.  The frozen content was
subsequently committed as
`c82f49be256f79fed45b7a4f1458d751d2ec0d9f`.  A postinspection rehash is
byte-for-byte identical to the preinspection list.

The verifier does not import or execute `model.py`, `exact_check.py`, or
`discover_affine.py` from the discovery package.  It separately implements
the census, canonicalization, automorphism orbits, transition coefficients,
finite-field elimination, source-count reconstruction, and exact witness
checks using Python's standard library.

## Complete independent census

Every labeled edge mask was tested through order seven.  Partitioning the
accepted masks into full permutation orbits gave:

| order | labeled admissible | unlabeled classes |
|---:|---:|---:|
| 1 | 1 | 1 |
| 2 | 2 | 2 |
| 3 | 8 | 4 |
| 4 | 57 | 9 |
| 5 | 683 | 21 |
| 6 | 13,174 | 62 |
| 7 | 394,020 | 208 |

Thus the independently reconstructed order-seven column set is complete for
the frozen local-admissibility criterion.

## Row inventory and coefficient semantics

The independent `6->7` matrix has shape `712 x 208`, split as:

```text
deletion                         62
vertex automorphism orbit       207
edge-pair automorphism orbit    180
nonedge-pair orbit              263
total                           712
```

For every rooted seven-class card, all canonicalizing isomorphisms were
checked to give the same orbit coefficient.  This made 20,408 isomorphism
choice checks.  A further 38,688 row/column partition checks confirmed:

- deletion coefficients count roots with the specified card;
- vertex-orbit coefficients partition all root-neighbor incidences;
- a pair coefficient `b_F,P(J)` counts pairs in orbit `P` whose two vertices
  are both adjacent to the deleted root, summed over all roots with card `F`.

Automorphisms are therefore only a lossless partition of distinguished
vertices and pairs in each small class.  No automorphism or transitivity
assumption is imposed on a putative 99-vertex graph.

## Independent source-six gate

Counts through order five passed all exact transition rows.  The independent
full-column-rank certificates were:

```text
1->2: rank  2 mod 3
2->3: rank  4 mod 2
3->4: rank  9 mod 5
4->5: rank 21 mod 3
```

I reconstructed the order-six vector from the frozen Wave-21 exact affine
formulas and its source-to-canonical bijection, rather than copying the
discovery module's constants.  It passes all 171 orbit-refined `5->6` rows.
That matrix has rank 61 modulo 7; adjoining the `n3` coordinate (canonical
mask `5941`) raises the rank to 62.  The source six-vector is therefore the
unique rational vector selected by the lower rows and `n3=705`.

## Rank and affine certificate

For the independently rebuilt matrix `A`:

```text
rank_F5(A)                    = 207
A delta                      = 0
delta                        != 0
rank_F5([A; H11-coordinate]) = 208
```

The modular lower bound and nonzero integer kernel vector together prove
`rank_Q(A)=207`.  The `H11` coordinate alone closes the 208-column system, so
the base at `H11=1412` and its kernel direction are uniquely determined
without any of the other Hamiltonian formulas.

Exact integer arithmetic independently established:

```text
A base  = b
A delta = 0
sum(base) = binom(99,7)
sum(delta) = 0
minimum at z=353 = 2     (support 208)
minimum at z=705 = 0     (support 207)
```

All 353 allowed integer values of `z` were explicitly replayed.  Every count
is integral and nonnegative.  Coordinatewise affinity and endpoint
nonnegativity also prove nonnegativity on the whole real interval.

## Hamiltonian comparison

The pinned Wave-22 chord lists were independently canonicalized and give 19
distinct admissible seven-classes.  `H_11` is the only coordinate required to
select a point on the one-dimensional family.  After selection, all 19
coordinates match the frozen Wave-21 affine source formulas at two adjacent
values, hence identically in `z`.

This verifies compatibility with the cited formulas.  It does not elevate the
human panel-to-mask transcription itself from `CITED` to independently
derived.

## Hostile tests and reproducibility

The independent suite passed `20/20` tests.  It rejects changed parameters,
missing or reordered lower records, changed lower counts, missing or
reordered seven-class records, altered base or kernel entries, Boolean values
masquerading as integers, row-count omissions, changed or hidden Hamiltonian
inputs, altered Hamiltonian records, hidden top-level assumptions, and
private Windows or POSIX absolute paths.

The verifier was run twice.  Both runs produced the byte-identical result:

```text
fecd813402ddb3aabd2434833f020edaf11e82c4392fc6ecc7d1f300b7d97eb4
```

The discovery package and generated public result pass the private-path scan.

## Scope wall

The certificate assigns aggregate counts to isomorphism classes.  It does not
make those assignments consistent across overlapping subsets and does not
provide a `99 x 99` adjacency matrix.  Accordingly:

```text
encoded necessary relaxation: VERIFIED FEASIBLE
graph construction:           NONE
new n3 bound:                 NONE
Conway-99 existence:          UNKNOWN
```
