# Clean-room protocol

1. Reconstruct the split-variable theorem from the verified Wave181,
   Wave188, Wave191, and Wave192 premises without opening Wave193 discovery
   files.
2. Freeze the independent mathematical result before reading the Wave193
   source manifest or source package.
3. Separate type-one, type-two, and type-three raw assignments by exact
   cross multiplicity and retain every raw capacity slack.
4. Audit the type-one exact-two residual lemma in both strict-containment and
   equality-to-checkerboard branches.
5. Prove that residuals generated after type-one or type-three exact-two raws
   have exact multiplicity one or three.
6. Charge old exact-three unused labels and the orbit-closed new residual
   pool without reusing circuit-label pairs.
7. Select at most one type-three leaf target per graph label and audit the
   complete low-circuit inventory, especially the coefficient one on `Y`.
8. Reproduce the exact dual identity for `117Q0-177C` and the integer null
   row.
9. Compare with the frozen source only after the independent result is
   sealed.
10. Keep the conditional lower bound separate from graph or code
    construction, endpoint exclusion, and Conway-99.
