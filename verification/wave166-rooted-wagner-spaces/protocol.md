# Wave 166 verifier protocol

1. Reconstruct the fixed-square degree equation from `lambda=1,mu=2`.
2. Check the endpoint edge totals and both Wagner shell orientations.
3. Validate the abstract `Z/9` shift shell and its no-Wagner property.
4. Reconstruct the fixed-nonedge sets `X,Y`, their sizes, and their exact
   20-edge count in each common-neighbor lane.
5. Audit the bijection with 40 marked induced five-cycles per nonedge.
6. Check the Wagner completion support pattern, eight-five-cycle
   multiplicity, and conditional endpoint arithmetic.
7. Explicitly attack the proposed four-failure bound and identify what the
   pair equations do not control.
8. Check the exact pair-root pseudowitness values against both new scalar
   inequalities.

The verifier must leave every sufficient but unproved lemma `UNKNOWN`.
