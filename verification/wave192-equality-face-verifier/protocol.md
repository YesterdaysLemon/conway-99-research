# Clean-room protocol

1. Reconstruct equality in the verified Wave191 certificate without opening
   the Wave192 discovery package.
2. Freeze the independent equality-face result and its SHA-256 before
   inspecting the Wave192 source manifest or files.
3. Derive every equality variable, including the one-parameter face and the
   exact-one, exact-two, raw-pool, residual-pool, and companion counts.
4. Audit saturation of the Wave181 canonical nonedge involution on the
   singleton-label subset.
5. For the zero type-three branch, reconstruct the two nontrivial affine
   axis words around a canonical checkerboard and prove that the unused axis
   word forces a new circuit.
6. For the positive type-three branch, use a nonprivate leaf word and the
   complete equality inventory to prove that no old circuit can lie in its
   support.
7. Compare the frozen independent statement with the source only after the
   mathematical payload is frozen.
8. Keep the strict circuit bound separate from graph or code construction,
   endpoint exclusion, and Conway-99.
