# Wave 40 orchestrator decision

Date: 2026-07-27 UTC

## Decision

Publish Wave 40 as a verified universal-rank breakthrough and a rigorous
endpoint-coupling checkpoint. Do not present it as a graph, counterexample,
endpoint exclusion, improved general upper bound, or literature-priority
claim.

Discovery did not certify itself. One verifier first reconstructed the
conditional rank-22 stepping stone and then, without importing the stronger
discovery implementation, rebuilt all eleven local types, the third-fibre
matching geometry, the arbitrary-completion border-rank lemma, and the exact
projective-subspace certificates. A different verifier independently rebuilt
the edge-type complex, quotient normalization, 39-block Laplacian identity,
and one-quotient full-lift census.

## Promoted mathematical result

For every hypothetical `srg(99,14,1,2)`, let `M=21E_0` be the integral
projector onto the zero eigenspace of the triangle-intersection graph. Then

```text
rank_F7(M)>=25.
```

For a local edge partition with `e` even parts, the 27-point block has rank
`25-2e`. The twelve vertices in the third triangle fibre form a permutation
of border signatures whose kernel projections have rank at least `e`.
Symmetric border completion therefore restores exactly the lost `2e` in the
rank lower bound.

The result is universal: it does not assume `n3=4158`, an automorphism, or a
particular local edge type. At the conditional prism-free endpoint it reduces
the arithmetic `(r3,r7)` census from 429 to 330 pairs. If `r3=12`, parity
forces even `r7>=26`.

## Scoped endpoint coupling

The endpoint edge types form faces of a closed two-dimensional incidence
complex with exact face and Euler identities. This is a genuine global
coupling, but the all-`222` case allows negative Euler characteristic and
survives.

Around one all-`222` base triangle, 4,050 normalized quotient forms are
exhaustive. Eight lie on the conditional ternary rank boundary. The full
39-point characteristic-seven rank is

```text
1+rank_F7(3I-A_X),
```

where `A_X` is the cubic 36-vertex neighbor core. The quotient omits eighteen
endpoint-pairing bits. For one boundary quotient, a complete `2^18` census
contains rank-33 triangle-free lifts. Consequently neither the quotient nor
this one-quotient census yields a universal rank-33 floor or an endpoint
contradiction.

## Prior-art boundary

The bounded Wave 40 source audit identifies Gray Taylor's 2020 public
notebook and repository as prior art for the 27-vertex edge neighborhood and
its eleven local types. The audit did not locate the exact universal
characteristic-seven rank-25 theorem or an equivalent statement, but bounded
non-discovery cannot establish novelty or priority. Both remain `UNKNOWN`.

## Continuation

The next exposed mathematical objects are:

1. the seven-primary Laplacian nullity of every admissible cubic
   triangle-neighbor core, not merely one quotient;
2. compatibility of the low-rank neighbor cores across adjacent base
   triangles;
3. a proof-producing completion of the remaining endpoint solver shards.

## Publication classification

```text
universal rank_F7(M)>=25:              VERIFIED
conditional endpoint rank pairs:       330
edge-type complex and quotient census:  VERIFIED SCOPED
one-quotient 2^18 lift census:          VERIFIED SCOPED
branch 15 and endpoint proof coverage:  UNKNOWN; 0/33
upper bound below n3<=4158:             NOT PROVED
target, novelty, and priority:          UNKNOWN
```
