# Wave 43 proof-agent report: type-`3+3` rank-two obstruction

## Assignment

Close the type-`3+3` blocker left by the conditional even-type rank-28 lane.
The target was the exact rank-two Schur-residual mechanism, without dense
enumeration or a completed-graph automorphism assumption.

## Result

Classification: **DERIVED, pending independent verification**.

For type `3+3`, the frozen border signatures vanish identically, so `F=0`
and local rank 27 is equivalent to residual rank two. A symmetric rank-two
matrix over `F_7` has an invertible principal `2 x 2` pivot. Enumerating every
pivot, deranged pivot-image pair, and pivot-mate configuration converts the
zero Schur complement into exact unary border domains and pairwise
mate/nonmate requirements.

```text
branches:                    666666
invertible pivots:           491220
unary-surviving branches:     60306
backtracking nodes:          122922
complete leaves:                  0
```

No type-`3+3` rank-two residual survives.

## Candidate combined consequence

If the separate Wave 43 even-type theorem is independently verified, all
four endpoint edge types `222`, `24`, `33`, and `6` have local
`rank_F7(K39)>=28`. Principal-block monotonicity would then give the
conditional endpoint theorem `rank_F7(M)>=28`.

The ceiling 44 remains compatible. This is not an endpoint exclusion or a
Conway-99 resolution.

## Verification request

Reconstruct the interaction table independently, prove the principal-pivot
lemma, reproduce the complete branch universe and zero-leaf result, and add
planted rank-two and hostile matching-degree controls.
