# Wave 27 runtime and agent-setting record

Recorded by the orchestrator on 2026-07-24.

## Exposed session settings

The Codex request metadata exposed to the root orchestrator reported:

```yaml
root_model: gpt-5.6-sol
root_reasoning_effort: ultra
sandbox: none
operating_system: Microsoft Windows 11 Home
os_version: 10.0.26200.0
python: 3.13.14
git: 2.51.0.windows.1
```

No credential, authentication token, cookie, or private chain-of-thought was
read or recorded.

## Subagent configuration

The orchestrator supplied no model or reasoning override when spawning the
Wave 27 discovery, construction, verification, and literature agents. The
collaboration runtime was therefore configured to inherit the root/default
session settings. The root did not receive separate child request-metadata
objects, so this file records the inherited configuration rather than
claiming a second observation of every child process.

Roles dispatched during the wave:

```text
construction:       A2-free h=9 arithmetic package
proof A:            unrestricted local E6 trace optimization
proof B:            E6/A6 cubic-tensor and ADE component screen
verifier 1:         independent arithmetic/root reconstruction
verifier 2:         independent E6 trace-theorem reconstruction
verifier 3:         independent tensor/CVP and ADE reconstruction
literature:         proof-separated source/status and overlap audit
orchestrator:       A20 trace-compression addendum and integration
```

Model confidence and agent agreement are not treated as certificates.
Mathematical promotion depends on the exact artifacts and independent verifier
reports cited in the claim and obligation ledgers.
