# Wave 206 Proof B: crossing localizers and a forced balanced tensor code

```yaml
role: proof_b
date_utc: 2026-07-29T21:48:45Z
git_commit: 85e705cc6c2a14d123120c93a847e30aaab1789e
claim_label: DERIVED
scope: >-
  Conditional prism-free n3=4158 centered rank-11 endpoint: derive exact
  fixed-middle compression coordinates, crossing/localizer contractions,
  and the common true-kernel consequence.
inputs:
  attempts/wave206-three-center-global-extension/protocol.md: 66c20334e284a26cc771ee86d97e678cb67b6efb4648b2af2fffe480fb5a9103
  verification/2026-07-29-wave205-integration-audit.md: f50b5591cb0e1e3dfdd835a22fd9156dd3a2a039323388889a6fa5e756a0a297
  agents/2026-07-29-wave191-global-star-module-proof-b.md: afe7d35627ec14a9599b427bcca0751e86a3f32b71fb7baf4d91e9e2e335f75f
method: >-
  Exact operator compression, K7 edge-coordinate trace form, crossing-feature
  contractions, true-kernel dimension comparison, and hostile controls.
command: >-
  .\.venv\Scripts\python.exe -B
  attempts\wave206-crossing-kernel-proof-b\exact_check.py --verify
  attempts\wave206-crossing-kernel-proof-b\exact-results.json ;
  .\.venv\Scripts\python.exe -B -m unittest -v
  attempts\wave206-crossing-kernel-proof-b\test_exact_check.py
outputs:
  attempts/wave206-crossing-kernel-proof-b/exact-results.json: 8c926d1275835767ccd7af722ac64921f80035655f305da2e4ad0e9a27571683
  attempts/wave206-crossing-kernel-proof-b/derivation.md: 9bdc0a296f704a5bd73ffa3a726694b74454b011dac486da9afa599f868aab13
limitations:
  - Independent Wave 206 verification is still required.
  - The controls are not shared-incidence endpoint realizations.
  - Conway-99 and every endpoint status remain UNKNOWN.
```

## Main coordinate theorem

For a fixed middle center `y`, put

```text
A_x^(y)=P_yP_xP_y restricted to E_y.
```

Then

```text
Tau^(y)[x,z]=tr(A_x^(y)A_z^(y)),
rank(Tau^(y))<=21.
```

The 21 entries

```text
m_x^(y)[i,j]=M_x[T_i,T_j], i<j,
```

on unordered pairs of distinct `y`-star triangles determine the entire
compression.  If `C` is the vertex--edge incidence matrix of `K_7`, then

```text
J_star=C^TC+2I_21,
det(J_star)=2,

g_xy=sum_e m_x^(y)[e],
tau_(xy;z)=(m_x^(y))^T J_star m_z^(y),
h_xy=(m_x^(y))^T J_star m_x^(y).
```

These entries are precisely the `y`-owned supported entries of `Q o M_x`.
Incidence supplies their locations, but their values and a center-sample
span theorem remain unknown.

## Characteristic-three correction

The ambient 21-dimensional trace form is nondegenerate, but its restriction
to a feature span need not be.  The trace-zero hyperplane has

```text
dimension 20,
rank 19,
radical <I_6>.
```

Thus a `Tau` Gram-kernel word is not automatically a true operator relation.
For any fixed trace residue, centered difference Grams have rank at most
19.  A largest nonneighbor residue class gives nullity at least eight, but
this remains a Gram statement.

## Crossing and global contraction

With `W[(T,U),x]=M_x[T,U]=w_TU[x]`,

```text
rank(W)=dim span{P_x}<=65,
W1=0,
W^TW=0,
(WW^T)^2=0.
```

The full contracted three-center matrix

```text
Gamma[x,y]=tr((Q o M_x)M_y)
```

satisfies

```text
Gamma=W^T diag(vec(Q))W
     =sum_z Tau^(z),
Gamma1=0,
diag(Gamma)=H1,
rank(Gamma)<=65.
```

## Forced nonconstant tensor-balance code

Let

```text
K_P=ker(c -> sum_x c_xP_x),
L=ker(B^T).
```

Since the projector target has dimension 65,

```text
dim(K_P)>=34.
```

The inherited bound `dim(L)<=33` and the true containment `L subset K_P`
force

```text
dim(K_P/L)>=1.
```

Equivalently the finite intersection code

```text
A_Delta=im(B^T) intersect ker(a -> D diag(a)D)
```

has dimension at least one.  It contains a nonzero word

```text
a=B^Tc,
sum_T a_T(z_T tensor z_T)=0,
(D o D)a=0.
```

The exact `G^2=G-J` argument proves

```text
im(B^T) intersect <1_231>={0},
```

so this word is nonconstant and is not merely the global frame coefficient.
If

```text
R_j=sum_(T:a_T=j) z_T tensor z_T,
```

then the weighted relation and the global frame zero imply

```text
R_0=R_1=R_2.
```

Projective distinctness of the quadratic Veronese columns gives
`wt(a)>=4`.  No stronger weight or composition bound is claimed.

## Hostile boundary

A deterministic control in the correct nonsquare 11-space has 99
rank-six projectors, 96 distinct, every one generated by a seven-column
singular simplex, and `sum P_x=0`.  It has

```text
rank(Tau^(y))=21 for all 99 y,
rank(Gamma)=64,
rank(H)=96.
```

It lacks the shared 231-column degree-three incidence and target graph.
Therefore it refutes only a generic slice-rank globalization.

The next exact invariant is the kernel of simultaneous compression across
all middle centers, or the mixed four-center tensor

```text
tr((P_yP_xP_y)(P_vP_zP_v)), y!=v.
```

No endpoint exclusion or Conway-99 resolution follows.
