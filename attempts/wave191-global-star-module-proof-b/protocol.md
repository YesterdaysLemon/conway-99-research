# Wave 191 protocol

1. Freeze the verified point-triangle identities and the rank-11 centered
   endpoint.
2. Work with all 99 stars simultaneously through `G=BB^T`, not through
   independent one-star profiles.
3. Split the 99-point module with the symmetric idempotent `E=G-J`.
4. Transfer `rank(B^TAB)=11` to the restricted form on
   `U_0=im(B) intersect ker(E)`.
5. Use nondegenerate orthogonal-complement dimensions to derive both the
   incidence-rank range and the exact dependency-code radical law.
6. Construct deterministic hyperbolic-space controls for every permitted
   dimension to distinguish a rank-only null boundary from an obstruction.
7. Do not present an abstract quadratic-space control as an incidence
   matrix, code realization, graph, or endpoint.
8. Record the complete line-sum weight enumerator and cubic/four-point trace
   tensor as unresolved.
9. Preserve `DERIVED` status until clean-room verification.
10. Keep rank-11 existence, novelty, and Conway-99 `UNKNOWN`.
