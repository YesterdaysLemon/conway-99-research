# Wave 128 finite-primary protocol

Claim label: `DERIVED` discovery; independent verification is required.

## Frozen scope

Assume only that a hypothetical `srg(99,14,1,2)` contains the induced
`C4` Cartesian `K3` motif frozen in Wave 105.  Import the forced incidence
matrix `P`, the corrected outside spectrum, and the Wave 109 lattice
identities by sealed manifest.  No automorphism of the unknown graph is
assumed.

Put

```text
Q=[one P],                 Lambda=ker_Z(Q^T),
B=D+4I,
U=Lambda intersect eig_Q(D,3),
K=Lambda intersect eig_Q(D,-4).
```

The target is to split the finite discriminant group of `Lambda` between
`U` and `K` at the determinant primes `2,3,5`, then combine it with the
characteristic-seven gluing index.  An obstruction is accepted only if an
exact local invariant or a checked finite certificate excludes a rank row.

## Separation and status rules

- This package is discovery, not verification.
- Finite-field elimination uses exact integer arithmetic only.
- A determinant or discriminant profile is necessary data, not a lattice
  construction and not an outside graph.
- `UNKNOWN` is retained for the motif extension, Conway-99, and novelty.
