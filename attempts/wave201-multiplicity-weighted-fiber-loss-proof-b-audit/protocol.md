# Wave 201 proof-B audit protocol

1. Preserve all sealed Wave196--201 source packages.
2. Expand full repetition separately for each leaf value in one fiber.
3. Compare selected multiplicity to full multiplicity value by value.
4. Check non-baseline and degree-five-baseline fibers, including empty,
   singleton, and multiple selected-value cases.
5. Use exactly the three verified degree-five pair types at `c_x=13`.
6. Treat every `c_x<=12` family without assuming an equality template.
7. Re-derive `delta>=3q-epsilon`, the global row, and the exact budget
   difference.
8. Run no graph, code, cover, SAT, LP, configuration, enumeration,
   isomorphism, or brute-force search.
9. Retain `DERIVED` pending clean-room verifier promotion.
