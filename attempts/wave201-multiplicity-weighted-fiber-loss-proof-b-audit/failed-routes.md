# Retained boundaries and hostile checks

## Aggregate repetition without leaf values

The inequality is unsafe if one merely says that repetitions add.
Expanding

```text
rho=sum_z(r_z-1)
```

separates each leaf value and proves that selected charges do not overlap.

## Blind baseline subtraction

Subtracting one from every fiber would be false. Exactly three baselines
are subtracted only at a 13-flag Hilton--Milner equality center, because
exactly three pair types there have degree five.

## Empty degree-five fiber

The algebra involving `k-1` assumes `k>=1`. When there is no selected
nonprivate value, the four-vertex cap and `d_P=5` directly give
`rho_P-1>=0`.

## Negative eta

The global row contains `-3eta`; it is not called a nonnegative
certificate. Its comparison with the full budget is valid because the
exact difference has coefficient `+8` on `eta` and no negative terms.

## Scope

No graph or finite-family search was performed. No endpoint
nonexistence, strict original `n3` improvement, or novelty claim follows.
