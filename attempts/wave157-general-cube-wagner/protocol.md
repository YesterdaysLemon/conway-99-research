# Wave157 general cube/Wagner symbolic-cut protocol

## Frozen question

Starting from a hypothetical `srg(99,14,1,2)`, the pointwise-labelled
four-root covariance construction for root mask 12, flag masks 21812 and
22708, and the independently transcribed six-set formulas in
`verification/wave43-seven-deck-endpoint/independent_check.py`, derive the
exact inequality valid for symbolic `n3` that involves:

- the order-eight cube class with canonical mask `2022000`; and
- the order-eight Wagner/Mobius-ladder class with canonical mask `5683824`.

The stored endpoint constant in
`attempts/wave152-four-root-order8/simplified-mask12-cut-2.json` is not an
assumption and must be independently recovered, if correct, by substitution
into the symbolic formula.

## Separation

- The target JSON and independent six-set checker were hashed before
  inspection.
- No discovery Python is imported or executed.
- Exact integer and rational arithmetic is required throughout.
- A finite or symbolic count inequality is not a graph, a strict upper bound,
  or a Conway-99 resolution unless the logical implication is separately
  proved.

## Resource boundary

At least 15% of host physical memory must remain free.
Initial free physical memory was 25.89%.

## Post-freeze scope

The proof package may read additional static source artifacts only after
recording their hashes. It must list every such input in the run report.

