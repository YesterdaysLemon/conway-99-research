# Retained boundary and limitations

## Looser local accounting

Counting only one unit of local label deficit per saturated 13-flag
center gives the valid source bound `s<=12`.  The independent audit keeps
the fiber losses separate and obtains `36-j_x>=3`, hence the stronger
global bound `s<=7`.

## What the contradiction excludes

The argument excludes only the integer face `Q=7037` inside the current
conditional pool system.  It does not construct or exclude the endpoint
graph itself and does not improve the original `n3` bound directly.

## Replay environment

Any source replay/import limitation is recorded after the independent
mathematical result freeze.  Such a limitation is not mathematical
evidence for or against the theorem.
