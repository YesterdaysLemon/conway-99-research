# Failed and stopping routes

## Coarse extension to `Q0=7039`

At `Q0=7039` the multiplied Wave198 budget is 103. In the lower-bound
argument, spending 99 units on the multiplicity deficit `epsilon` can
erase the forced saturated count. The present inequality therefore does
not exclude that face.

## Non-additive deficient-center cap

A first estimate bounded each deficient center by seven saturated pair
types. Tracking repeated leaf loss is much stronger and additive:
`delta_x>=4s_x`.

## Search

No graph, flag-family, configuration, SAT, LP, or isomorphism search was
used. Absence of a stronger conclusion is not presented as nonexistence.
