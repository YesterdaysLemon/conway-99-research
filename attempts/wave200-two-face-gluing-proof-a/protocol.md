# Wave 200 proof-A protocol

1. Leave all sealed Waves196--199 packages immutable.
2. Treat the two candidate integers `Q0=7037,7038` uniformly through a
   budget parameter `B<=63`.
3. Reconstruct the oriented `q,s,epsilon,delta,eta` identities.
4. Count leaf-union loss additively across disjoint four-element fibers.
5. Use the verified Hilton--Milner equality templates only at `c_x=13`.
6. Record why the same coarse mechanism stops at `Q0=7039`.
7. Run no graph, code, cover, SAT, LP, configuration, enumeration,
   isomorphism, or brute-force search.
8. Keep the result `DERIVED` pending independent audits.
