# Failed and incomplete routes

## Category-summed integer lift

The exact lift `z=Ax/3`, `Az=4x-z+2t*1` is stronger pointwise than the
ternary check.  Summing it over `P` and `N`, however, reproduces the existing
first/second signed moments: the residuals reduce identically to zero using
`p-n=3t`.  No endpoint composition can be removed by that aggregation alone.

## Balanced weight fourteen

The two Farkas inequalities remove every unbalanced weight-fourteen
composition but vanish on enough local types to leave `(p,n)=(7,7)` open.
The submitted aggregate control meets all retained moment and local-matching
sums.  It is intentionally nongraphical, so it is neither positive evidence
for a word nor a counterexample.  It shows only that a proof needs graphical
degree compatibility, neighbor-to-neighbor lift correlations, endpoint
incidence geometry, or another genuinely stronger input.

## MacWilliams identities

Length `99`, dimension `54`, and the existence of parameter-forced
weight-24 kernel words do not determine the complete weight enumerator or the
dual distribution.  No coefficient was set to zero without those missing
inputs, and no MacWilliams-based exclusion is claimed.

## Higher endpoint weights

The same first/second-moment feasibility system has surviving local types at
weights `17`, `20`, and `23`.  This run did not obtain a nonnegative
pointwise certificate with a negative global sum for any entire weight, and
it did not search for a graph.  All three weights remain `UNKNOWN`.

