# Wave 78 protocol

1. Import only the signed-support and outside-moment statements explicitly
   named from Waves 71 and 74.
2. Do not assume an automorphism, transitivity, a canonical support, or a
   completed graph.
3. Prove the neighborhood-size bounds by inclusion-exclusion, then reproduce
   them by exhaustive four-subset enumeration.
4. Enumerate every bounded integer histogram exactly.
5. Keep a histogram distinct from a labelled block design and from a graph.
6. Discovery cannot promote its own result to `VERIFIED`.
7. Retain Conway-99 and novelty as `UNKNOWN`.
