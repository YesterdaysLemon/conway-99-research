# Wave 197 proof-A protocol

1. Leave sealed Wave195 and Wave196 packages immutable.
2. Fix one nonedge and count the five anticomplete leaf triangles in each
   orientation using only `lambda=1,mu=2`.
3. Use minimal-cover privacy to exclude selecting both exact-three
   companions of one flag support.
4. Separate private `p3` labels from the remaining selected exact-three
   label union before applying the degree-ten cap.
5. Add `a3+b3` as distinct oriented labels outside the selected union.
6. Include selected, old-raw, and new-residual exact-three pairs in the
   Wave196 flag cap.
7. Keep `SH=3h-a3-b3` distinct from residual capacity `RA`.
8. Expand the final certificate after all four raw identities.
9. Preserve the rational null row as arithmetic only.
10. Run no graph, code, cover, SAT, LP, configuration, enumeration, or
    isomorphism search.
11. Keep `Q>=7033` labeled `DERIVED` pending an independent verifier.
