# Wave 209 proof-A protocol: rank-three signed trade

## Frozen import

Import only the manifest-pinned Wave 208 rank-three conclusion used by the
Wave 209 global protocol: a hypothetical prism-free rank-11 endpoint yields
an integer vector

```text
c in {0,+1,-1}^99,  Ac=3c,
```

with either support 14 and five selected opposite-sign line intersections,
or support 20 and two such intersections.  The selected product-one graph is
the labelled `K_(4,4)` minus its labelled perfect matching.  The full target
remains `UNKNOWN`.

## Allowed methods

1. Derive exact point-signature and common-neighbor moments from
   `(99,14,1,2)`.
2. Use the complete 231-line partial-quadrangle incidence identities.
3. Enumerate integer moment compositions, the 12 labelled product-one pairs,
   and abstract seven-point support types without identifying labels by a
   target automorphism.
4. Use a complete bounded dynamic program only as an explicitly stated
   selected-union relaxation.  A surviving relaxation row is not a graph.

## Forbidden promotion

No restricted non-hit, aggregate signature row, solver status, induced local
control, or line-type census is a 99-vertex certificate.  A global exclusion
requires a proof covering both support sizes; a counterexample requires all
99 vertices and every SRG equation.

