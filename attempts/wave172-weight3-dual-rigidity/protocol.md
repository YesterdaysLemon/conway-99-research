# Protocol

1. Freeze the prism-free endpoint and the verified integer reflection.
2. Work with a symbolic weight-three kernel vector; do not enumerate graphs
   or block triples.
3. Separate the two ternary coefficient orbits before using integer lifts.
4. Use both `C^2=441I` and `C*1=-21*1`.
5. Translate a mod-three residue to an integer entry only after invoking the
   endpoint entry alphabet.
6. Record joint symbol counts rather than promoting an ordinary enumerator.
7. Retain `UNKNOWN` unless the marked constraints themselves become
   infeasible and receive independent verification.
