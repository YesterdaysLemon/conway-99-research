# Wave 99 protocol

## Frozen scope

Assume a hypothetical `srg(99,14,1,2)` at the prism-free endpoint
`n3=4158`, equivalently `P=0`.

Imported, independently verified facts:

- the rooted `K14-7K2` residual-label scaffold and its 84 selected
  transitions;
- the prism-free transition restriction;
- the complementary-Fano norm-14 support and rooted-seed injection;
- the Wave 86 full level-seven coefficient identity in the additional
  rank-28 or `q=16` row.

The norm-14 theorem itself does not assume rank 28. The weighted
norm-16/norm-18 corollary does.

## New question

Can the pairwise overlap of selected transitions force more than the 168 bad
four-point seeds used by Wave 90?

## Claim boundary

- Discovery label: `DERIVED`.
- Independent verification is required before promotion.
- `N14` counts `t` and `-t` separately.
- No upper bound on `N16` or `N18` is claimed.
- No endpoint, rank row, graph, or Conway-99 resolution is claimed.
