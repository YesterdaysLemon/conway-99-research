# Wave 40 rank-25 literature package

This package records a bounded primary-source literature and priority audit for
the independently verified universal necessary condition

```text
rank_F7(M) = rank_F7(J-I-2A) >= 25
```

for any hypothetical `srg(99,14,1,2)`, with `M=21E0` the integral
triangle-projector matrix.

## Verdict

- Exact/equivalent rank-25 statement located: **no, in the bounded corpus**.
- Exact prior art for the 27-vertex edge-neighborhood and its 11 types:
  **yes**, Gray Taylor's 2020 pinned notebook and post.
- General `p`-rank/Smith-form treatment of `aA+bJ+cI`: **yes**, Brouwer and
  van Eijl (1992).
- Novelty: **UNKNOWN**.
- Priority: **UNKNOWN**.

No absent search hit is used as evidence of novelty.

## Files

- `source-ledger.json`: primary sources, exact overlap classifications, pins,
  and negative-scope statements.
- `query-log.json`: search windows, query strings, and bounded dispositions.
- `input-freeze.sha256`: hashes of the verified Wave 40 statement used as the
  audit target.
- `run-report.yaml`: protocol-compliant run metadata.
- `package-manifest.sha256`: SHA-256 manifest of this package, excluding the
  manifest itself.

## Reproduction

The direct GitHub checks use authenticated `gh` code search and pinned raw
content. PDF term checks were performed on the primary PDFs listed in the
source ledger. Query nonhits are not completeness certificates.
