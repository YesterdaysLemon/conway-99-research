# Wave 209 protocol: globalize or exclude the four M7g survivors

## Frozen conditional frontier

Assume a hypothetical prism-free rank-11 endpoint for an
`srg(99,14,1,2)` and a weight-eight word in `A_Delta`.  Import only the
manifest-pinned Wave 208 conclusions after their stated verification gates:

1. exactly four of the 27 labelled M7g polar forms survive the integer
   mod-nine obstruction;
2. the unique rank-three form gives an ordinary integer vector
   `c=B_S alpha` with `Ac=3c`, entries in `{0,+1,-1}`, and either
   `(support, selected intersections)=(20,2)` or `(14,5)`;
3. each of the three rank-four forms gives an integer vector
   `q=(A-3I)c/3` with `Aq=-4q`, `q.q=56`, and selected triangle sums
   `U^T q=-3 alpha` (up to one consistent global sign convention);
4. restricted 19-, 22-, and 23-vertex controls are not completions.

The target is a global proof of impossibility or a complete machine-readable
99-vertex counterexample.  Any lesser result must remain a conditional
reduction with an explicit unresolved boundary.

## Lane A: rank-three signed trade

Work from the exact equation `Ac=3c` and the two signed-unit support sizes.
Derive all outside neighbor-signature counts, triangle-incidence moments,
and consequences of `lambda=1`, `mu=2`, and the 231-line partial-quadrangle
structure.  Seek a symbolic contradiction, an exact classification, or a
complete extension certificate.

Required controls:

* retain both support 14 and support 20;
* do not identify labelled intersection subsets by an unproved
  automorphism;
* an aggregate signature table is not a graph;
* a restricted non-hit is not nonexistence.

## Lane B: rank-four norm-56 eigenvector

Use the exact integral `-4` eigenvector and all selected triangle sums.
Exploit the full triangle incidence operator (`BB^T=A+7I`), integral
eigenlattices, line-sum moments, spectral projectors, and congruences.  Seek
a symbolic contradiction or a complete exact realization.

Required controls:

* retain all three labelled rank-four forms and all 83 marked subsets per
  form until a proved equivalence or exclusion applies;
* do not extrapolate the repository's norm-14/16/18/20 classifications to
  norm 56;
* theta-series feasibility is necessary, not sufficient;
* no target automorphism may be assumed.

## Verifier gate and status wall

Discovery agents use `DERIVED`, `CANDIDATE`, `REFUTED`, or `UNKNOWN` only.
The source-blind verifier must receive sealed manifests and reproduce each
promoted claim independently.  Unless all 99 vertices and every SRG
equation are certified, Conway-99 remains `UNKNOWN`.

