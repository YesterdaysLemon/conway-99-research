# Wave 196 proof-A protocol

1. Leave both sealed Wave195 packages immutable.
2. Include selected, old-raw, and genuinely new residual exact-three
   companion pairs in the flag pool.
3. Derive the seven-edge matching on `N(x)` from `lambda=1`.
4. Prove every two-star-block nonedge type has exactly four vertices.
5. Derive the `ij,ik,jk` fiber types of one canonical flag.
6. Retain the weight-two and weight-three dual-distance exclusions making
   the local `A`-family simple and intersecting.
7. Use the exact two Hilton--Milner equality templates at `(7,3)`, not an
   unstated uniqueness assertion.
8. In the common-star branch, prove the leaf triangle is uniquely
   determined by its `p`-neighbor via `mu=2` and `lambda=1`.
9. Count distinct oriented labels, including opposite orientations of one
   type-two private nonedge.
10. Expand the final certificate after the four raw identities.
11. Preserve the integer null row as arithmetic only.
12. Run no graph, code, cover, SAT, LP, configuration, enumeration, or
    isomorphism search.
13. Keep `Q>=7029` labeled `DERIVED` pending an independent verifier.
