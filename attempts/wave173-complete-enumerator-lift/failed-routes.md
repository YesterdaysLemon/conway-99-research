# Scope and retained boundary

## This is not yet an endpoint contradiction

Wave 54 supplied one formal ordinary enumerator as a positive control.  It
did not classify all ordinary weight enumerators satisfying the recorded
constraints.  Refuting its complete lift removes that control but leaves open
the possibility of another ordinary distribution.

That possibility survives the rational degree-three relaxation.  The exact
six-cell control in `general-rational-control.md` satisfies total code size,
all degree-one and degree-two projective zeros, the Wave 172 mixed
degree-three zero, and exactly 231 distinguished `(162,36)` scalar pairs.
Its multiplicities and `B30` are rational, so it is neither an integral
enumerator nor a code.

## Numerical infeasibility is unnecessary

A small complete-composition LP is infeasible, but no solver status is used
as evidence.  Equations (3)--(7) are an exact rational Farkas-style
certificate.

## Individual complete coefficients matter

Ordinary `B3=120` alone does not imply `B30=60`.  The step uses Wave 172's
independently verified theorem `B21=B12=0` and scalar symmetry.  Dropping
that marked premise invalidates the contradiction.

## Live continuation

The next exact problem is to characterize all ordinary enumerators consistent
with the established code constraints, while retaining the degree-three
marked zero.  A successful endpoint exclusion must either prove that every
ordinary candidate violates the same moment bound or derive additional
marked constraints from the 99 point-star dependencies.
