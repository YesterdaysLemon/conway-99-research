# Wave 64 protocol

1. Freeze `AGENTS.md`, the verified Wave-3 relation-algebra source, the
   Wave-35 endpoint artifact, and the available Wave-62 verifier artifacts
   before writing discovery code.
2. Use the rooted scaffold only as a labeling device.  Assume no automorphism
   of a completed graph.
3. Reconstruct all finite counts from definitions.
4. Treat HiGHS and MiniCard only as candidate finders.  Check a witness as an
   explicit integer object; do not infer nonexistence from a status or nonhit.
5. Distinguish the exact transition/design structure, the block-only master,
   the stronger linear master, and the complete residual-codegree formulation.
6. Discovery labels its own successful finite claims `DERIVED`, never
   `VERIFIED`.
7. Keep endpoint and Conway status `UNKNOWN` without a checked complete graph
   or a checked proof of nonexistence.

