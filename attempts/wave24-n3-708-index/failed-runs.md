# Wave 24 retained run ledger

## 1. Discarded dense floating scan

Before the analytic logarithmic inequality was found, an exploratory Python
loop sampled equal positive/negative eigenvalue groups on roughly 200,000
grid points for every pair of multiplicities.  The shell killed the process
after its 20-second command budget (reported elapsed time about 24 seconds).

The scan was floating point, incomplete, and produced no result.  It is not
used anywhere in the proof or checker.  A smaller exploratory grid suggested
the candidate cap `3^8`, after which the report's calculus proof and exact
characteristic-polynomial argument replaced numerical search entirely.

## 2. Exact replay

The first exact suite completed without a mathematical or harness failure:

```text
python -B -m unittest -v test_exact_check.py
Ran 15 tests
OK
```

The submitted JSON was then generated by:

```text
python -B exact_check.py --output exact-results.json
```

Neither successful command is evidence that a graph exists; they check only
the finite arithmetic and the explicit relaxation described in the report.
