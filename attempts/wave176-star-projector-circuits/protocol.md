# Protocol

1. Work conditionally only on the verified endpoint branch and centered
   rank `k=11`.
2. Treat the seven-point star Gram simplex as the source of the projector;
   do not assume projectors commute.
3. Derive the four adjacent cycle types from the SRG parameters and `P=0`.
4. Distinguish Gram nullity, restriction radical, and true column
   relations.
5. Use `r-g<=11-r` before promoting a Gram-kernel word to a dual relation.
6. Enumerate only the at most 27 words in a derived three-dimensional
   kernel; perform no graph, code, SAT, or isomorphism search.
7. Retain pairwise compatibility and trace-rank gaps as `UNKNOWN`.
8. Require an independent verifier before changing package status.
