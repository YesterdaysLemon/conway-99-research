# Protocol

1. Freeze the Wave 174 dual-distance and Wave 176--177 star-relation inputs.
2. Prove support multiplicity directly from triangle incidence and
   `lambda=1`; do not assume the 693 Wave 177 choices are distinct.
3. Pass to inclusion-minimal dependent sub-supports before using the word
   `circuit`.
4. Distinguish true column-relation kernels from larger Gram kernels.
5. Use the Gram kernel only as a superset to classify possible short true
   relations.
6. Limit exact enumeration to the four derived `13 x 13` local matrices,
   with at most `3^5=243` kernel vectors per type.
7. Perform no graph, code, SAT, configuration, or isomorphism search.
8. Require an independent verifier before promotion.
