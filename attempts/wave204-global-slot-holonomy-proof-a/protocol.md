# Protocol

1. Freeze the scope to the conditional prism-free endpoint
   `n3=4158`, `P=0`, and the ternary rank-11 branch.
2. Treat `b=0` only as the surviving Wave 203 aggregate/local-interface face;
   do not call it a graph, code, cover, or flag-family construction.
3. Use the globally normalized exact-three flag relation before defining any
   gain.
4. Keep graph centers, triangle blocks, and five-slot charts as different
   object types.
5. Define an honest transition only when an existing flag gives actual
   triangle blocks `S,T`; do not extend the partial map to five slots.
6. Compose two flag transitions only when the target block of the first is
   literally the source block of the second.
7. Attach no canonical-quadrilateral 2-cell from the Gram kernel alone.
8. Check true block cycles and projected center cycles separately for lengths
   3, 4, and 5.
9. Build exact relaxed controls in the correct nonsquare 11-dimensional
   ternary form and list every endpoint premise they do not realize.
10. Retain the Conway-99 and rank-11 status as `UNKNOWN`; proof A cannot
    promote its own work to `VERIFIED`.
