# Wave 194 proof-A protocol

1. Freeze the verified Wave181, Wave188, Wave189, and Wave191 premises.
2. Split every raw assignment by selected source and exact multiplicity.
3. For an exact-three type-two raw, cancel its unique leaf coordinate in
   the containing `6+2` or `2+6` relation and extract a residual minimally.
4. Exclude the raw, its companion, the selected conic, and exact
   multiplicity two from that residual.
5. Audit every exact-one collision: different private labels separate by
   exact multiplicity; for the same type-two label, intersection with the
   opposite raw has weight at most three, and two residuals have
   intersection support of weight at most two. Use opposite centers to
   separate exact-three circuits and companion pairs.
6. Split the genuinely new residual pool into exact-one circuits `y` and
   exact-three companion pairs `g`; do not replace them by a coarse average.
7. In the selected-type-three label union, introduce the actual selected
   type-two incidence `k` and cancel it between inside and outside counts.
8. Charge only type-three raw exact-one circuits `c1`, not all `r1`, to
   type-three-union leaf targets.
9. Keep every circuit pool disjoint by privacy, exact-two uniqueness,
   center orientation, and companion closure.
10. Verify the final result through an exact rational coefficient identity;
    do not use a numerical optimizer as proof.
11. Derive the full equality face from zero certificate terms plus the
    unused exact-two and exact-three capacity rows.
12. Treat every equality row and packet as arithmetic/accounting only.
13. Run no graph, code, cover, SAT, LP, configuration, enumeration, or
    isomorphism search.
14. Leave the result `DERIVED` until a separate clean-room verifier
    promotes it.
