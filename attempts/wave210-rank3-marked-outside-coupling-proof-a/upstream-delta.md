# Upstream delta after the initial freeze

The Wave 209 rank-three verifier sealed after this lane began.  Its outer
manifest hash is now pinned in `input-freeze.sha256`.

That verifier confirms the 352 weight-20 rows as an aggregate necessary
catalog and separately derives a `352 -> 346` narrowing after an additional
selected-line degree cap.  The narrowing remains labelled `DERIVED` pending
another independent verifier.  This Wave 210 lane neither reproduces nor
imports it because its exact scope is the weight-14 branch.  No frozen
weight-14 input changed.

