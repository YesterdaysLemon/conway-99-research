# Wave 189 retained boundaries

1. The Wave 187 sparse control had negative `B_(7,0)`, but it violated the
   star-local polygon at several primal cells.  It cannot certify a universal
   degree-seven obstruction.
2. The adjacent/nonadjacent star-intersection pattern forces useful complete
   rows only in total degrees 12--14.  It creates no additional pure
   total-degree-seven row beyond the 99 stars and their negatives.
3. The new 16-cell control has only a rational three-point Gram census.
   No integral census, code, point set, or graph is claimed.
4. The first disjoint-pool estimate gave `Q>=4158`; companion closure and
   coupled assignment packing improved it to `Q>=4620`; center-oriented
   type-two supports improved it to `Q>=4851`.  These are retained as
   superseded scalar checkpoints.
5. A signed-incidence/cohomology contradiction on the `Q=4851` equality
   face was not needed.  Direct subtraction of the forced checkerboard conic
   from a leaf relation already creates an extra circuit and gives the
   strict bound `Q>=4852`.
