# Wave 74 failed routes and boundary

## Aggregate moments beyond \(h=2\)

The outside moment equations completely eliminate norm-18 \(h=2\), but they
leave four norm-16 histograms, 20 norm-18 \(h=0\) histograms, and six
norm-18 \(h=1\) histograms.  The presence of an integer histogram is not a
graph construction, but its presence blocks a contradiction from these two
moments alone.

## Cross-pair total

Summing common-neighbor capacities over \(P\times N\) reproduces
\(\sum_xd_x^2=2\sum_x\binom{d_x}{2}+\sum_xd_x\).  After the same-sign
equations are imposed, this is an identity and supplies no new scalar cut.

## Local matching shortcut

Because \(\lambda=1\), every vertex neighborhood is a perfect matching.
For an outside vertex this constrains the support graph induced on its
\(2d_x\) support neighbors, but the aggregate histograms do not determine
that induced matching.  No elimination is claimed without a complete
compatibility check.

## Status wall

Norm-18 \(h=2\) is `DERIVED` impossible.  All other branches remain live.
Conway-99 and novelty remain `UNKNOWN`.
