# Protocol

1. Assume only the verified prism-free endpoint and Wave 172's classification
   of a possible weight-three dual word.
2. Re-derive `D=B^T*A*B` over `F_3`.
3. Convert the dependency into a point--triangle line-sum coloring.
4. Derive every five-cell count from the SRG equations; assume no
   automorphism or equitable partition.
5. Use exact interlacing and integer convexity, not a graph or code search.
6. Derive the final prism directly from equality conditions.
7. Have a verifier reconstruct the argument without importing the discovery
   checker.
8. Promote only dual distance at least four; retain the endpoint and
   Conway-99 as `UNKNOWN`.
