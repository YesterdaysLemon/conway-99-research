# Wave 100 protocol

## Frozen imports

- verified `n3+3P=4158`;
- verified rooted prism multiplicity `sum_o f_o=6P`;
- Wave 99 discovery's unverified local pair-moment lemma: with
  `m_o=84-f_o` non-mate transitions, the bad-seed count is at least
  `ceil(5m_o/2)`.

## Quantifiers

The proposed `N14` bound applies to every hypothetical target graph and
assumes neither prism-freeness, rank 28, nor an automorphism.

## Status

Wave 100 is `DERIVED`, not verified. It must not be promoted before the
Wave 99 local lemma and this global floor/parity argument receive separated
verification.

The theorem bounds only `N14`; `N16`, `N18`, a strict `n3` upper bound, and
Conway-99 remain unresolved.
