# Wave 109 discovery protocol

Role: construction / alternative-space discovery.

The target is conditional: assume a hypothetical `srg(99,14,1,2)` contains
the induced `C4` Cartesian `K3` motif frozen and independently checked in
Wave 105. Rebuild its forced `87 x 12` outside-incidence matrix `P`
independently, then study the primitive kernel lattice

```text
Q=[one P],             Lambda=ker_Z(Q^T).
```

Required checks:

1. prove the rank, primitivity, and exact Gram determinant of `Lambda`;
2. determine the characteristic-seven orthogonal type without inheriting a
   sign convention;
3. prove that `D+4I` restricts to a rational projector on `Lambda`;
4. derive its local characteristic-seven rank from the global Seidel rank by
   an exact Schur complement;
5. record only Smith and index consequences that follow from the projector
   identity;
6. test every imported global rank row and state explicitly whether any row
   is excluded.

The package must not use the Wave 105 linear witness as a graph, must not
promote its own claims to `VERIFIED`, and must leave the motif, full graph,
Conway-99, and literature novelty `UNKNOWN` absent an exact contradiction.
