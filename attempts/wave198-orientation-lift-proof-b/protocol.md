# Wave 198 protocol

1. Keep the two orientations of every selected type-three label separate.
2. Apply capacity five to each occupied orientation.
3. Use privacy only for `s_e=t_e=1` on the `p3` labels.
4. Add old-raw orientations only after verified separation from `U`.
5. Couple to the verified Wave196 full oriented union and flag cap.
6. Record explicitly why the Wave197 active row fails the new slack.
7. Reproduce the final identity with exact rational arithmetic.
8. Retain `DERIVED` status pending independent verification.
