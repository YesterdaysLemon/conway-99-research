# Wave 41 equality boundary and retained limitations

## No local rank-25 positive control

The complete seven-type search found no perfect matching `R` satisfying the
singular Schur equality for any minimum-projection permutation `F`. Therefore
this package cannot provide the requested rank-25 positive control. The null
result is a finite local exclusion, with the complete cover and atomic
outputs recorded in `README.md`.

## Rank 26 is not a graph contradiction

The new candidate floor `rank_F7(M)>=26` is compatible with the remaining
global rank arithmetic. It does not force a prism, exclude `n3=4158`, or
improve the general upper bound `n3<=4158`. Treating a one-unit rank
improvement as a solution would inflate status.

## Local blocks do not add

The preceding multi-edge package exhibits three independent vertex-star
kernel vectors in every 39-point triangle block. Those vectors are killed by
all outside columns. Therefore ranks of multiple local blocks cannot be
summed without controlling their overlap and the nonconstant kernel
directions.

## The next equality level is larger

Excluding rank 25 only proves rank at least 26. A rank-26 classification
would have to retain one additional rank direction in the singular
congruence. Equation (1) would be replaced by a low-rank condition on the
right-kernel bilinear form, not by equality to zero. The present target hash
lookup does not certify that larger search.

## No novelty claim

No literature comparison was performed in this lane. Priority and novelty
remain `UNKNOWN` even if an independent verifier confirms the mathematics.
