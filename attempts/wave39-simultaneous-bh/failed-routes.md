# Wave 39 simultaneous B/H failed routes and exact boundary

All statements concern the hypothetical prism-free endpoint `n3=4158`.
Nothing here establishes existence or nonexistence.

## Centered polar positivity survives

At the surviving `r3=12` boundary, the 231 centered rows are distinct
isotropic projective points in a nonsquare eleven-space.  Their exact
oriented inner distribution is

```text
[1,0,32,162,36].
```

The complete five-relation scheme has Delsarte transforms

```text
231, 209/135, 13/27, 493/1107, 23/9.
```

Every transform is positive.  Hence the ordinary two-point association
scheme cannot exclude this boundary.  Any continuation must use the 99
distinguished zero-sum seven-cliques, higher-point semidefinite constraints,
or the graph completion equations.

## Low-weight dual words are not yet a code classification

The centered projective self-orthogonal `[231,11]_3` code would have

```text
A_198>=462,
B_7>=198,
B_12>=1386,
B_13>=1386,
B_14>=16632.
```

These exact inequalities are strong input for a future integral
MacWilliams/linear-programming attack, but no complete feasible enumerator
or infeasibility certificate is supplied here.

## Aggregate B/H counts reproduce, rather than beat, the spectral moments

The simultaneous overlap census implies

```text
triangles(H)=32,
C4(H)=171+C4(A_X).
```

These agree exactly with the independently verified spectral-transfer
identities.  Thus the aggregate counts alone give no contradiction.

For the frozen rank-ten local core, `C4(A_X)=4`, and all forced aggregate
counts remain nonnegative.  No sixty-column `B`, graph `H`, or fractional
completion was found; absence of one is not claimed.

## Boundary

The smallest sharpened completion problem now includes:

1. a sixty-column binary `B` with the exact Gram and block-overlap census;
2. 96 overlap-zero pairs partitioned into 32 edge-disjoint triples;
3. 144 overlap-one pairs partitioned into 36 labeled four-edge matchings;
4. per-column triangle degree `1+e_y`; and
5. the full `BH` and `YY` equations.

The rank-twelve case, simultaneous completion, endpoint, and upper-bound
improvement all remain `UNKNOWN`.
