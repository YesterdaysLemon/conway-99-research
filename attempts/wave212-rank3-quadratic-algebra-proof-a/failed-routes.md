# Failed routes and scope walls

- The full mod-2 rank sequence determines the nontrivial Artin--Schreier
  Jordan blocks, but does not determine whether each block belongs to the
  `0`- or `1`-primary part of `D`.  The conditional algebraic multiplicities
  `45` and `40` admit allocations consistent with the elementary parity
  checks.
- The conditional characteristic polynomial is exactly `x` times a square
  modulo two, as required for an odd-order alternating adjacency matrix.  It
  gives no contradiction.
- Every conditional `K`-projector diagonal lies strictly inside `(0,1)`.
  No zero or unit local multiplicity forces a row.
- For all 3,570 pairs in every orbit, both binary values pass the `2 x 2` PSD
  minors of both conditional `K` projectors.  Cauchy--Schwarz gives no edge
  cut.
- A discovery-only triangle-hypergraph scout was stopped when the parallel
  proof-B lane produced strictly stronger labelled controls.  Its weaker
  generated controls were deliberately excluded from this sealed package to
  avoid duplicated evidence and status inflation.

No solver exit code or incomplete search is used as evidence.  No target
automorphism is assumed.

