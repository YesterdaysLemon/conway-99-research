# Wave 208 global residual rigidity: frozen protocol

## Global target and status wall

Determine whether a simple graph `srg(99,14,1,2)` exists.  The only accepted
terminal outcomes are a complete machine-checkable graph certificate or a
complete independently verified nonexistence proof.  At the start of this
wave:

```text
Conway-99:                    UNKNOWN
rank-11 prism-free endpoint:  UNKNOWN
rigorous n3 interval:         708<=n3<=4158
conditional Q bound:          Q>=7059
```

No automorphism is assumed.  A local or aggregate non-hit has no proof status.

## Frozen Wave 207 implications

Conditional on a hypothetical weight-eight word

```text
a in A_Delta=im(B^T) intersect ker(a -> D diag(a) D),
```

the following are independently verified:

1. `Da=Za=0`; the support is a `4+4` `M7g` configuration.
2. For `a=B^T c`, `b=Ba` is nonzero in `ker_F3(A)` and
   `wt(b) in {14,17,20,23}`.
3. Parameter-only, `d(ker_F3(A))>=12`.
4. A weight-14 kernel word, if one exists, has sign composition `7+7`.
5. Exactly four of the 27 normalized restricted polar forms are excluded;
   23 remain.
6. The 23-vertex rank-four control checks only `A_U b_U=0`; the other 76
   point-code coordinates and every outside completion law are absent.
7. Features linear in `P_yP_xP_y` annihilate true projector relations and
   cannot classify the word by themselves.

## Lane A: point-code integer lift

For `x in ker_F3(A)` represented in `{0,1,-1}`, write

```text
P={x=1}, N={x=-1}, p=|P|, n=|N|, p-n=3t,
z=Ax/3 in Z^99.
```

The exact integer lift is

```text
Az=4x-z+2t*1.
```

Retain individual and edge-correlated `(x_v,z_v)` data, graphical degree
compatibility, local `7K_2` structure, and exact spectral identities.  Do not
reuse category sums already proved redundant.  Target a complete exclusion
or classification of balanced weight 14 and weights 17, 20, and 23.

## Lane B: marked M7g global incidence

Retain product-one ambiguity: selected triangles with polar product one may
intersect or be disjoint with one cross edge.  Impose consequences visible
beyond the 23-vertex union, including all 99 equations `Ab=0`, outside
degree/common-neighbor sums, the 231-triangle clique graph, and the relation
`a=B^Tc`.  A restricted intersection model must list every restriction and
cannot prove nonexistence without a complete checked cover.

## Lane C: code/enumerator and hostile controls

Derive only parameter-forced weight-enumerator, dual, projection, or
shortening consequences.  Unknown code coefficients may not be set to zero.
An exact feasible aggregate object is a boundary control, not a code or graph.
An infeasible relaxation is evidentiary only with a retained exact dual
certificate whose premises cover the claimed branch.

## Verification gate

A source-blind verifier freezes its own claims and tests before reading any
Wave 208 discovery package.  Discovery agents label results `DERIVED` or
`CANDIDATE`; only the verifier and orchestrator may promote scoped results.

## Inflection criteria

This wave is globally decisive only if it supplies one of:

1. a verified target graph;
2. a complete verified nonexistence proof;
3. a verified exclusion of every rank-11 prism-free endpoint branch plus a
   separately valid amplification to the unrestricted target.

Otherwise retain `UNKNOWN` and record the strongest exact reduction and the
smallest surviving obstruction.
