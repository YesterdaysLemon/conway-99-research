# Audit protocol

1. Freeze dual distance, canonical companion flags, and Wave194 pool
   separation.
2. Prove the four-element fiber bijection directly from `lambda=1`,
   `mu=2`, and the local matching.
3. Derive the flag fiber types as a loopless degree-two multigraph.
4. Inspect both Hilton--Milner equality templates and count only forced
   repeats in distinct fibers.
5. Prove the common-star injection using `mu=2` and `lambda=1`.
6. Include `g` only after checking verified residual-orbit separation.
7. Reproduce the coefficient certificate with exact rational arithmetic.
8. Retain `DERIVED` status pending a verifier.
