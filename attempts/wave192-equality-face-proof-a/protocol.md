# Wave 192 proof-A protocol

1. Freeze the independently verified Wave191 package and its direct
   canonical-C4 and affine-word premises.
2. Reconstruct every equality slack; do not assume the displayed Wave191
   arithmetic row is compatible with unused structural lemmas.
3. Separate residual assignment incidence `Z` from closed-pool circuit
   count `Y`.
4. Exclude exact-two residuals label by label before assigning new-pool
   capacity.
5. Record the full one-parameter equality family before attempting to
   exclude it.
6. Use the canonical nonedge involution only when a four-block support is
   already known dependent.
7. In affine circuit elimination, prove that neither known circuit support
   is contained in the residual support before declaring a new circuit.
8. Apply the exact-three leaf exclusion to nonprivate labels only after
   checking that privacy was not used in its local proof.
9. Keep projective circuits separate from nonminimal relation words.
10. Run no graph, code, cover, SAT, LP, configuration, enumeration, or
    isomorphism search.
11. Leave `Q>=6238` as `DERIVED` until a separate verifier promotes it.
