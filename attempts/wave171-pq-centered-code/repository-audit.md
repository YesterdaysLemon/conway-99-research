# Repository audit at Wave 171

## Blunt status

The target remains `UNKNOWN`.  The strongest rigorous interval found in the
tracked state is

```text
708<=n3<=4158,
n3+3P=4158.
```

Thus the hard upper endpoint is equivalent to `P=0`.  No inspected artifact
constructs the graph, excludes that endpoint, or improves the upper bound.

## Reproduced and retained

- The Wave 157/158 general cube--Wagner inequality and its verifier replay
  remain exact.
- The Wave 165 cube upper bound and Waves 166--167 defect identities reduce
  endpoint exclusion to a genuinely new estimate such as
  `E<=8`, equivalently `N_Wagner>=18710`; current local equations do not
  prove it.
- Wave 170's pointwise block profile is consistent with the Wave 171
  square-zero polynomial and exact row composition.
- The targeted package manifests through Wave 170 rehash cleanly.
- The focused Wave 157/158 suite passes all 15 tests; the repository-wide
  `code` suite passes all 57 tests in the current checkout.

## Scope corrections

- `attempts/wave163-coupled-conic-dual/` and
  `attempts/wave164-triangle-geometry-motif/` are explicitly
  `DRAFT_UNEXECUTED`.  They are strategies, not results.
- Wave 169's five-failure gadget refutes only the present pairwise/root-local
  proof strategy.  It is not a complete SRG assignment or a nonexistence
  certificate.
- Wave 171's code is not independent new evidence: `X=2(C+J)` identifies it
  exactly with the prior centered-code lane.
- The statement that all targeted sources retain the problem as unresolved
  is a bounded source audit, not a proof of openness or novelty.

## Ledger lag

The narrative `README.md` integrates work through Wave 171, while
`STATUS.yaml` and `CLAIMS.yaml` end their detailed wave ledger at Wave 147.
Those formal ledgers are therefore incomplete as indexes of the current
tracked research state, although their top-level target status remains
correct.  This checkpoint records the lag rather than pretending to repair
24 waves of historical metadata without a dedicated integration audit.

## Recommended continuation

The best low-computation lane is a marked or partition weight-enumerator
argument for the ternary centered code.  It must use both:

1. the 231 distinguished weight-198 rows of the symmetric square-zero matrix
   `X`; and
2. the 99 canonical seven-row point-star dependencies and their prescribed
   intersections.

Ordinary MacWilliams/Pless moments and unmarked support-design arguments have
already saturated.  A useful next theorem would derive a contradiction in
the `r3=12` endpoint branch, or prove a rank improvement, from these marked
incidence constraints.  Until such a theorem is obtained and independently
checked, this remains a proposed specialization rather than evidence for
either existence outcome.
