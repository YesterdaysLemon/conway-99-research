# Protocol

1. Freeze the unrestricted `srg(99,14,1,2)` target and the convention
   `n3+3P=4158`.
2. Use the full 99-point, 231-block incidence geometry; impose no
   automorphism, transitivity, or canonical-label assumption.
3. Derive the matrix polynomial over `F_3` from the SRG identity before
   using endpoint data.
4. Count every row symbol from the exact Wave 170 block profile.
5. Compare with the Wave 20/35 projector before invoking `P=0`; the
   polynomial/centered-code identity holds without the endpoint assumption.
6. Distinguish an intrinsic reformulation of an existing centered code from
   a new obstruction.
7. Check primary theorem text rather than promoting an abstract's use of
   the word "classification".
8. Retain `UNKNOWN` unless an independently checked construction or
   nonexistence proof is produced.
