# Wave 186 protocol

1. Freeze the independently verified dual-distance, cross-capacity,
   multiplicity-three, and multiplicity-two inputs.
2. Reconstruct translated relation supports symbolically; do not search for
   a graph or a code.
3. Use proper-star independence to force every circuit inside a translated
   support to be cross-star.
4. Use private labels only when declaring a forced circuit outside the
   selected cover.
5. Allow maximum overlap: one outside circuit may receive three private-label
   assignments.
6. Retain the distinct Wave 180 triple companions as a separate lower bound.
7. Prove the final `8/9` inequality coefficientwise and keep the endpoint
   and Conway-99 status `UNKNOWN`.
