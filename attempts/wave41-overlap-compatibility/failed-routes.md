# Wave 41 overlap failed routes

## 1. The forced 19-vertex overlap is compatible

Two canonical rank-33 all-`222` blocks agree exactly on the overlap arising
from the adjacent base triangles `(0,1,2)` and `(0,3,4)`. Therefore that
induced overlap alone gives no contradiction.

## 2. Raw kernel projection remains too small

After exact two-per-fibre balance and the individual common-neighbor support
cap, the twenty new columns can have joint kernel-signature span one. The
border-congruence floor is only 35, not 45.

## 3. A sampled union rank is not a theorem

Preliminary heuristic choices for the unforced cross-exclusive adjacency
gave high 59-by-59 ranks. Those choices are neither forced nor exhaustive.
They were discarded and no sampled rank is used as evidence of a
contradiction.

## 4. Individual columns are not a block design

The explicit one-dimensional-signature witness chooses each column
independently. It need not realize the forced `BB^T` pair concurrences or a
compatible outside graph. It is a positive control for the first-order
relaxation only.
