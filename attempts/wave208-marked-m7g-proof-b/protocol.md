# Wave 208 proof B: marked M7g spectral-divisibility protocol

## Frozen scope

Work conditionally at the prism-free rank-11 endpoint of a hypothetical
`srg(99,14,1,2)`.  Let `B` be its point--triangle incidence matrix and let

```text
a in im_F3(B^T)
```

be a hypothetical weight-eight tensor-balanced word on an `M7g` support.
The unique projective tensor relation, all four concurrent-secant perfect
matchings of the labelled support, and all 27 restricted polar forms are
retained.  The proof does not choose a secant matching.  A polar entry one
may mean either a selected-triangle intersection or a disjoint pair with one
cross edge.  No automorphism of a hypothetical graph is assumed.

The orchestrator protocol was frozen before this lane at SHA256

```text
3f3def2dd84610fd352d257b0b7d5b014f160901f63e6c2b91038e4734053740.
```

## Method gate

1. Lift the eight nonzero ternary coefficients to integers in `{+1,-1}` and
   use the real spectrum `14,3,-4`, not a 99-vertex construction search.
2. Keep actual selected-triangle intersections distinct from polar-product
   one throughout.
3. Enumerate only the unique tensor relation, 27 three-parameter forms, and
   their at-most-12 marked intersection edges.  Every such census is
   label-complete and has no graph-symmetry premise.
4. A positive local model is a hostile control only.  It omits all outside
   vertices and cannot establish a graph completion.
5. Discovery results remain `DERIVED` until a separate verifier replays them.

## Status wall

Unless every surviving form and every unrestricted endpoint branch is closed,
retain

```text
weight-eight A_Delta word: UNKNOWN
rank-11 endpoint:          UNKNOWN
Conway-99:                 UNKNOWN
```
