# Candidate quarantine

Unverified matrices, partial completions, local configurations, and solver
models belong here. A filename or solver status never implies that an object is
a Conway 99-graph. Promotion requires the independent process in
`verification/` and an update to `STATUS.yaml`.
